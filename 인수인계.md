# 법령 조문 캡처 → 한글 삽입 자동화 프로그램 (law_capture)

> **상태 (2026-08-07): v1.0.0 동작. GitHub 배포됨. 조문별 시행일 버그·미리보기 찾기/밑줄 클릭 삭제·실행.bat 반영(미커밋 변경 있음).**
> 위치 `C:\Users\USER\Desktop\DEV\law_capture_to_hwp\`, 실행 `실행.bat` 또는 `법령캡처_실행.pyw`, 사용법 `README.md`.
> 규칙 `.cursorrules`. GitHub `https://github.com/JWP9412/law_capture_to_hwp`
> **다른 도구(Claude Code 등)로 이어받는다면 바로 아래 '인수인계' 절부터 읽으세요.**

---

# 인수인계 (Claude Code / Cursor 이어받기)

## 이 프로그램이 하는 일

건설 하자소송 준비서면을 쓸 때, 국가법령정보센터에서 **사건 시점에 시행 중이던** 법령 조문을 찾아 → PDF로 받고 → 쟁점 문구에 빨간 밑줄을 긋고 → 그림으로 잘라 → 한글 문서에 캡션·테두리와 함께 넣습니다. 손으로 하면 조문당 3~5분 걸리는 일입니다.

**사용자:** 개발자가 아니라 건축 전공자. 주석·화면·오류는 한국어로, 전문용어는 풀어서.

## 지금 바로 쓸 수 있습니다

```bash
cd C:\Users\USER\Desktop\DEV\law_capture_to_hwp
# 더블클릭: 실행.bat  (한글 경로 깨짐 방지 → run.pyw → launch.py)
# 오류 볼 때: 실행_오류확인.bat
python 법령캡처_실행.pyw
```

```bash
# 명령줄로 (검증할 때 편함)
python run_from_command_line.py --name 하자판정기준 --date 2025-06-01 ^
    --article 1 --underline "제1조(목적) 이 기준은" --out "out\시험.hwp"
```

## Claude Code가 먼저 할 일

1. `.cursorrules` + **이 인수인계 절** 읽기
2. `git status` / `git diff` — v1.0.0 이후 **미커밋 변경**이 있음 (아래 목록)
3. 손대기 전: `python -X utf8 -u spike/check_staggered_effective_dates.py` 통과 확인
4. **한글 프로세스 강제 종료 금지**

### 미커밋으로 남아 있을 수 있는 것 (작업 전 git status 로 확인)

| 파일 | 내용 |
|---|---|
| `core/law_source.py` | 조문별 시행일 — 특정 시점·기간·현행 표시 수정 |
| `ui/input_view.py` | 기준일 기본값 = 오늘, 칩↔미리보기 밑줄 동기화 |
| `ui/preview_view.py` | 헤더 [찾기], 빨간 표시 클릭 삭제 |
| `spike/check_staggered_effective_dates.py` | 소방시설법 검증 (신규) |
| `spike/check_gui.py` | 찾기·밑줄 삭제 검증 |
| `README.md` | 찾기·밑줄 클릭 안내 |
| `실행.bat`, `실행_오류확인.bat`, `run.pyw`, `launch.py` | 실행 진입점 |
| `법령캡처_실행.pyw` | launch 경유 |

## 파일 구조

```
law_capture_to_hwp/
  main.py / 법령캡처_실행.pyw / run.pyw / launch.py / 실행.bat
  config.py          APP_VERSION = "1.0.0"
  core/
    law_source.py      검색 + 연혁 + 시점·기간 판 고르기 (조문별 시행일 주의)
    law_pdf.py / article_text.py / favorites.py
    annotate.py / hwp_insert.py / version_series.py / pipeline.py
    law_site.py / models.py / errors.py / article_number.py
  ui/
    app_window.py  input_view.py  preview_view.py
    favorites_dialog.py  progress_view.py  result_view.py  controller.py
  spike/             검증 스크립트
  docs/VERSION_RELEASE_CHECKLIST.md
  00.CHANGELOG/ 00.README/ 00.PROJECT_STRUCTURE/  (버전별 신규 파일)
```

## 검증 스크립트 (spike/)

한글을 켜는 것과 안 켜는 것을 구분. **한글을 켜는 것은 꼭 필요할 때만.**

| 스크립트 | 한글 | 확인 내용 |
|---|---|---|
| **`check_staggered_effective_dates.py`** | X | **조문별 시행일·소방시설법 (필수)** |
| `check_law_source.py` | X | 검색·개정 이력·시점별 판 |
| `check_statute.py` | X | 법령(법률·시행령) |
| `check_article_text.py` | X | 미리보기·전체보기·원문 URL |
| `check_favorites.py` / `check_favorites_dialog.py` | X | 즐겨찾기 |
| `check_timing_modes.py` | X | 시점 UI 셋·홈네트워크 경계·주택법 172개 |
| `check_gui.py` | X | 화면·찾기 버튼·밑줄 클릭 삭제 |
| `check_law_pdf.py` / `check_annotate.py` / `check_multipage.py` | X | PDF·밑줄·긴 조문 |
| `check_hwp_insert.py` 등 | **O** | 한글 삽입·격리·실실행·`render_hwp` |

한글 검증은 **시간 제한(Job Timeout)** 필수. 대화상자가 뜨면 영원히 멈춤.

```powershell
$job = Start-Job -ScriptBlock {
  Set-Location 'C:\Users\USER\Desktop\DEV\law_capture_to_hwp'
  python -X utf8 -u spike\check_multipage.py 2>&1
}
Wait-Job $job -Timeout 150 | Out-Null
Receive-Job $job
if ($job.State -eq 'Running') { Write-Output "[멈춤]"; Stop-Job $job }
Remove-Job $job -Force
```

## 절대 하면 안 되는 것

**`Get-Process -Name Hwp* | Stop-Process`** — 사용자 한글 문서가 날아감. 정리가 필요하면 **창 제목이 있는 한글이 하나라도 있으면 중단**.

- `CoUninitialize()` 호출 금지
- `pyhwpx`의 `set_font` / `move_caption` 금지
- `HAction.Run("...Dialog")` 금지 → GetDefault → Execute

## 이 환경의 특이사항 (모르면 반드시 막힘)

1. **회사 보안이 한글 로컬 저장을 막음** — 바탕화면·내 문서는 조용히 실패. `%TEMP%` 저장 → 한글 종료 → 파이썬이 옮김.
2. **별도 스레드에서 한글:** `pythoncom.CoInitialize()` 필수. **`CoUninitialize()` 금지.**
3. **기존 한글 열 때 `lock:false`.**
4. **법령/고시 PDF 저장 방식이 다름** → 폼 전송 가로채기로 통일.

## 코드 작성 원칙

- `core/`는 화면 모름, `ui/`는 법령·한글 모름. 잇는 곳은 `ui/controller.py`만.
- `core/` 실패 시 예외. 잡는 곳은 `pipeline.run_capture_jobs` 한곳.
- 함수 매개변수 3개 이하. 불리언은 `is_`/`has_`/`can_`. 약어 금지.
- 주석에 **왜 이 방법인지** (특히 조문별 시행일·한글 COM).

## 2026-08-07 핵심 변경 — 조문별 시행일

### 증상 (고침)

소방시설 설치 및 관리에 관한 법률:

| 경로 | 예전(버그) | 지금 |
|---|---|---|
| 검색 | `236977` / 2024-12-01 | 동일 |
| 특정 시점 | 잘못 `247269` / 2023-07-04 | `236977` / **2024-12-01 (현행)** |
| 기간 2024~2026 | “개정본 없음” | 기간 안 늦은 날로 포함 |

### 원인

연혁에서 같은 문서번호에 시행일이 여러 개(예: 2022-12-01 + 2024-12-01)인데, 중복 제거가 **이른 날만** 남김. 기간·특정 시점이 그 날만 보면 검색과 어긋남.

### 수정 (`core/law_source.py`)

| 함수 | 역할 |
|---|---|
| `_load_history_with_all_dates` | 대표 목록(이른 날) + `version_id → 시행일 집합` |
| `_collect_effective_dates_by_version_id` | 검색 API 시행일도 병합 |
| `_effective_date_on_or_before` | 기준일 이하 중 최댓값 |
| `_pick_version_for_reference_date` | 특정 시점·현행 표시 |
| `find_version_effective_on` | 조문별 시행일 전체로 고름 |
| `find_versions_effective_between` | 기간 안 시행일 **하나라도** 있으면 포함 |

목록 정렬용 대표일은 여전히 이른 날. **고르기·현행·기간 표시**만 전체 시행일 사용.

### 기준일 UI

`ui/input_view.py`: 기본값 **오늘** (`date.today()`). 칸은 **수정 가능** (사건 시점 조회). 완전 잠금 아님.  
예전 기본값 `{올해}-06-01`이 `2026-06-01`처럼 보였던 원인.

## 2026-08 미리보기·실행 UX

| 기능 | 요점 |
|---|---|
| **찾기 버튼** | 미리보기 헤더, [원문 보기] 왼쪽. Ctrl+F와 동일 막대 |
| **밑줄 취소** | 빨간 표시 **클릭** → 그 밑줄만 삭제. 왼쪽 칩과 양방향 |
| **실행.bat** | bat 안 한글 금지(CP949 깨짐). `run.pyw`만 호출. 실패 시 `실행_오류.log` |

## 2026-07-31 — 즐겨찾기 · 전체보기 · 원문 보기

| 기능 | 요점 |
|---|---|
| **즐겨찾기** | `user_favorites.json`. Combobox + [설정] (`ui/favorites_dialog.py`) |
| **전체보기** | `fetch_full_law_text`. 드래그 시 조문 번호 자동. PDF는 조문 지정 그대로 |
| **원문 보기** | `LawVersion.detail_page_url` (법령은 `efYd`) |

## 남은 작업 / 배포

- 버전은 **1.0.0**. 다음 배포 시 `docs/VERSION_RELEASE_CHECKLIST.md` 준수 (`*_vX.Y.Z.md` **신규**, 덮어쓰기 금지).
- 위 미커밋 변경 커밋·푸시는 사용자 요청 시에만.
- 단일 EXE 배포는 비현실적(한글·Playwright Chromium). 폴더형만 검토 수준.

## 참고 — 검증에 쓸 실제 데이터

| 용도 | 값 |
|---|---|
| 고시(짧은 조문) | 하자판정기준 / 2025-06-01 / 제1조 / 밑줄 `제1조(목적) 이 기준은` |
| 법령(짧은 조문) | 공동주택관리법 / 2025-06-01 / 제39조 / 밑줄 `하자담보책임` |
| **조문별 시행일** | 소방시설 설치 및 관리에 관한 법률 → 오늘 기준 **시행 2024. 12. 1.** |
| **긴 조문(3쪽)** | 주택법 / 2011-01-28 / 제2조 |
| **가지번호** | 주택건설기준 등에 관한 규정 / 제32조의2 |
| **시점 경계** | 지능형 홈네트워크 / `2012-09-20` → 시행 2011. 3. 4. |
| **개정본 많음** | 주택법 전체 기간 → 172개 |

## 사용자에게 보고할 때

- 전문용어 풀어서. 검증했다고 말하려면 **실제로 돌릴 것**.
- 한글 결과물은 `render_hwp.py`로 눈으로 확인.

---

> 아래는 원래 계획서·과거 인수인계 상세입니다. 설계 근거 참고용.

## 과거 기록 — 2026-07-28 고친 것 (검증 완료)

| 문제 | 어디 |
|---|---|
| 화면에서 실행 시 한글이 안 열림 (`CoInitialize`) | `core/hwp_insert.py` |
| 긴 조문 뒷부분이 통째로 잘림 | `core/annotate.py`, `core/pipeline.py` |
| 본문 추출이 짧은 호를 버림 | `core/annotate.py` |
| 사용자 한글을 종료해 작업이 날아감 | `core/hwp_insert.py` |
| 우리가 켠 한글이 안 꺼짐 | `core/hwp_insert.py` `_quit_hwp` |
| 캡션 번호 글자 겹침 → 자동번호 필드 | `core/hwp_insert.py` 등 |
| '어느 시점?' 세 가지 모드 | `ui/input_view.py` |

## Context

건설 하자소송 준비서면·의견서를 쓸 때 반복되는 수작업이 있습니다.

1. 국가법령정보센터에서 **사건 발생 시점에 시행 중이던** 법령/고시 버전을 찾고
2. 해당 조문 왼쪽 체크박스를 켜서 PDF로 저장하고
3. PDF를 열어 쟁점 문구에 빨간 밑줄·빨간 테두리를 긋고
4. 화면을 캡처해서
5. 한글 보고서에 붙여넣고 크기 조절 → 테두리 → `[그림 10] …` 캡션 번호까지 손으로 맞춤

조문 하나당 3~5분, 서면 한 건에 10~20개면 한 시간이 사라집니다. 게다가 **시점을 틀리면 치명적**입니다 — 예시로 주신 「공동주택 하자의 조사, 보수비용 산정 및 하자판정기준」만 해도 현행이 시행 2026.7.8.(제2026-360호)인데, 2025년 사건에는 시행 2025.2.3.(제2025-58호) 버전을 인용해야 하고 이 둘은 별개 문서로 저장됩니다.

이 전 과정을 tkinter GUI 한 번 실행으로 끝내는 것이 목표입니다.

---

## 코드 작성 원칙 (클린 코드)

아래 다섯 가지를 모든 `core/`·`ui/` 파일에 동일하게 적용합니다. 앞서 잡아둔 함수 시그니처 중 이 기준에 안 맞는 것들을 여기서 다시 정리합니다.

### 1. 의미 있는 이름 — 애매한 동사·명사를 전부 구체화

| 이전 초안 (애매함) | 확정 이름 (의도가 드러남) |
|---|---|
| `resolve(query, base_date)` | `find_version_effective_on(law_name, reference_date)` |
| `download_article_pdf(...)` | 유지 (이미 명확함) |
| `annotate_and_capture(...)` | `underline_and_capture_page(...)` — "annotate"는 밑줄 외에 뭘 하는지 안 드러남 |
| `LawShot` (파이프라인 작업 단위) | `ArticleCaptureJob` — "무엇의 캡처인지"가 이름에 들어가야 함 |
| `is_current` (플래그) | `is_currently_in_effect` — 불리언은 `is_`/`has_` + 형용사절로 통일 |

원칙: **약어를 안 씀** (`ef_date` → `effective_date`, `jo_no` → `article_number`), **불리언은 반드시 `is_`/`has_`/`can_`으로 시작**, **함수 이름은 동사로 시작해 하는 일을 그대로 읽으면 되게** (`get_law_text_from_mcp`처럼 "무엇을 가져오는지"까지).

### 2. 단일 책임 원칙 — 파이프라인 하나의 함수가 여러 일을 안 하게

이전 초안의 `pipeline.py`는 "버전 찾기 → PDF 받기 → 밑줄 긋기 → 한글 삽입"을 한 함수 안에서 순서대로 호출하는 그림이었습니다. 이걸 **단계마다 별도 함수 + 각 단계는 자기 실패만 책임지는 구조**로 바꿉니다.

```python
# core/pipeline.py — 오케스트레이터는 "순서를 정하는 일"만 함. 각 단계 내용은 모름.
def run_capture_job(job: ArticleCaptureJob, on_progress: ProgressCallback) -> CaptureJobResult:
    version = find_version_effective_on(job.law_name, job.reference_date)
    on_progress(JobStage.VERSION_RESOLVED, version)

    pdf_path = download_article_pdf(version, job.article_number)
    on_progress(JobStage.PDF_DOWNLOADED, pdf_path)

    image_path = underline_and_capture_page(pdf_path, job.underline_phrases)
    on_progress(JobStage.IMAGE_ANNOTATED, image_path)

    insert_captured_image(job.target_hwp_path, image_path, build_caption(version, job))
    on_progress(JobStage.INSERTED, None)

    return CaptureJobResult.succeeded(job, image_path)
```

- `find_version_effective_on`은 **버전을 찾는 것만** 압니다. PDF를 어떻게 받는지 모릅니다.
- `download_article_pdf`는 **PDF를 받는 것만** 압니다. 밑줄을 어떻게 긋는지 모릅니다.
- 이렇게 나누면 "PDF 다운로드가 이상해요"라는 문제가 생겼을 때 `law_pdf.py` 한 파일, 많아야 몇십 줄만 보면 됩니다.

`ui/controller.py`도 마찬가지로 "버튼 이벤트를 core에 넘기고, core가 돌려준 진행 상황을 화면 갱신 함수에 넘기는 것"만 합니다 — 화면을 그리지도, 법령을 검색하지도 않습니다.

### 3. 매개변수 최소화 — 관련 값은 객체로 묶기

이전 초안의 `annotate_and_capture(pdf, underline_texts, box_texts, png_out)`처럼 4개 넘는 함수가 여럿 있었습니다. **관련된 값들을 데이터 클래스로 묶어서 함수는 최대 3개까지만** 받게 정리합니다.

```python
@dataclass(frozen=True)
class ArticleCaptureJob:
    law_name: str
    reference_date: date
    article_number: int
    underline_phrases: list[str]
    target_hwp_path: Path
    insertion_mode: InsertionMode   # PLACEHOLDER | APPEND_TO_END

@dataclass(frozen=True)
class LawVersion:
    source_kind: LawSourceKind      # ADMINISTRATIVE_RULE | STATUTE
    version_id: str
    law_name: str
    effective_date: date
    promulgation_label: str
    detail_page_url: str
    is_currently_in_effect: bool

def underline_and_capture_page(pdf_path: Path, phrases: list[str]) -> Path: ...
def insert_captured_image(hwp_path: Path, image_path: Path, caption: ImageCaption) -> None: ...
```

`ArticleCaptureJob` 하나가 GUI 입력 화면의 "담은 목록" 한 줄과 그대로 대응됩니다 — 화면에 보이는 항목과 코드 안의 데이터 구조가 1:1이라 나중에 봐도 헷갈리지 않습니다.

### 4. 주석 — 비전공자도 코드를 따라 읽을 수 있을 만큼 자세히

일반적인 클린 코드 원칙은 "이름으로 다 설명되면 주석은 군더더기"라고 하지만, **이 프로젝트는 개발자가 아닌 분이 나중에 코드를 열어볼 가능성이 있으므로 원칙을 조정합니다.** 함수 이름만 보고 "무슨 일을 하는지"는 알 수 있어도 "그 일을 왜 이런 방식으로 하는지"나 "이 코드가 실제로 어떤 순서로 움직이는지"는 프로그래밍을 모르면 안 보일 수 있습니다. 그래서:

- 각 함수 맨 위에는 **그 함수가 하는 일을 한국어 한두 문장으로**, 마치 옆에서 설명해드리듯이 씁니다 (파이썬의 `"""...""" ` 방식)
- 코드 중간에 **여러 단계로 나뉘는 부분은 각 단계 앞에 "지금 뭘 하는 중인지"를 한 줄로** 남깁니다 — 코드를 몰라도 주석만 읽으면 전체 흐름이 읽히게
- **왜 이렇게 짰는지(다른 방법이 아니라 이 방법을 고른 이유)**는 특히 자세히 남깁니다 — 이게 없으면 나중에 "왜 굳이 이렇게 복잡하게 짰지?" 하고 코드를 잘못 "정리"하다가 버그를 만들 수 있습니다

```python
def apply_border_via_image_editing(image: Image) -> Image:
    """
    캡처한 그림 가장자리에 검은 테두리를 그려 넣는다.
    (두 번째 참고 사진처럼, 한글 문서 안에서 그림이 박스로 감싸진 것처럼 보이게 하는 부분)

    보통 이런 테두리는 한글 프로그램 안에서 "그림 속성 → 테두리 설정"으로 넣지만,
    이 프로그램은 그 방법을 쓰지 않는다. 한글을 사람 손 없이 자동으로 조작할 때,
    "그림 속성" 창을 여는 명령을 코드로 호출하면 실제로 대화상자(팝업창)가 화면에
    뜨면서 프로그램이 그 자리에서 멈춰버리는 것을 1단계 테스트에서 확인했다.
    (사람이 없으니 그 팝업창의 "확인" 버튼을 눌러줄 수가 없어서 영원히 멈춤)

    그래서 대신, 한글에 넣기 전 단계인 "그림 파일(PNG)" 상태에서 미리
    사진 편집하듯 테두리 선을 그려 넣는다. 한글 입장에서는 그냥
    "테두리가 그려진 그림 파일"을 받는 것뿐이라 문제없이 동작한다.
    """
    ...
```

### 5. 에러 처리 — "실패 목록에 담는다"를 구체적인 예외 체계로

이전 초안엔 "밑줄 문구를 못 찾으면 에러 목록에 담는다"까지만 있고 정작 어떤 예외를 어디서 던지고 누가 잡는지가 없었습니다. 예외 계층을 명시적으로 정의합니다.

```python
class LawCaptureError(Exception):
    """이 프로그램에서 발생하는 모든 오류의 최상위 타입."""

class LawVersionNotFoundError(LawCaptureError):
    def __init__(self, law_name: str, reference_date: date):
        super().__init__(f"'{law_name}'의 {reference_date} 기준 시행 버전을 찾을 수 없습니다")

class ArticlePdfDownloadTimeoutError(LawCaptureError): ...
class UnderlinePhraseNotFoundError(LawCaptureError):
    def __init__(self, phrase: str, pdf_path: Path):
        self.phrase = phrase          # 화면에서 "어느 문구가 실패했는지" 보여줄 때 씀
        super().__init__(f"'{phrase}' 문구를 {pdf_path.name}에서 찾지 못했습니다")

class HwpAutomationError(LawCaptureError): ...
```

**규칙:**
- `core/` 안의 함수는 실패하면 **`None`이나 `False`를 반환하지 않고 반드시 위 예외 중 하나를 던짐** — 호출한 쪽이 "성공인지 확인하는 if문"을 계속 반복하지 않게
- 예외를 잡는 곳은 **`core/pipeline.py`의 `run_capture_job` 딱 한 군데**뿐 — 한 조문 작업이 실패해도 나머지 조문은 계속 처리되도록, 잡은 예외를 `CaptureJobResult.failed(job, error)`로 변환해서 돌려줌 (화면3의 "✗ 제10조 — … 문구를 찾지 못함"이 여기서 나옴)
- `ui/controller.py`는 `CaptureJobResult`만 보고 화면을 갱신함 — 예외를 직접 잡지 않음 (이미 `core`에서 값으로 변환됐으므로)
- 스택 트레이스는 로그 파일에만 남기고 **화면에는 `str(error)`의 한국어 문장만** 노출 (위 예외 클래스들이 생성자에서 한국어 메시지를 만드는 이유)

---

## 사용 언어 및 이유

**Python 3.10**을 씁니다 (이 컴퓨터에 이미 깔려 있는 버전, court_monitor도 이걸로 되어 있음).

다른 언어를 고르지 않은 이유:
- 한글(HWP)을 코드로 조작하는 통로(`pyhwpx`)가 파이썬에만 있고, 이미 이 컴퓨터에 설치돼 있음
- PDF에 빨간 밑줄 긋는 도구(PyMuPDF), 웹사이트 자동 클릭 도구(Playwright)도 이미 설치돼 있어 **추가로 배울 것도 설치할 것도 없음**
- court_monitor(지금 이 폴더의 법원 조회 프로그램)와 완전히 같은 조합 — 만약 나중에 유지보수를 다른 사람에게 맡기더라도 같은 방식이라 설명하기 쉬움

**화면(GUI)은 tkinter**를 씁니다. 파이썬에 기본으로 들어있는 가장 단순한 창 도구입니다. court_monitor의 `run_gui.pyw`를 더블클릭해서 쓰시는 것과 완전히 같은 방식 — 새 프로그램도 아이콘 더블클릭 하나로 켜집니다. 설치 파일이나 인터넷 서버 같은 게 따로 필요 없습니다.

---

## 화면 구성 계획 (UI/UX)

건축 도면과 비슷하게 생각하시면 됩니다 — "이 화면에서 저 화면으로 어떻게 넘어가는지"를 먼저 정하고, 그 다음 각 화면 안에 뭐가 있는지를 정합니다.

### 전체 흐름 (창 하나, 3가지 상태만 존재)

```
[대기 상태]                [실행 중 상태]              [완료 상태]
조건 입력          →       진행바 + 로그       →       "한글 파일 열기" 버튼
"실행" 버튼 누름            "중지" 버튼만 가능           "다시 실행" 가능
```

창을 여러 개 띄우지 않습니다. 위 세 상태를 **한 창 안에서 내용만 바뀌는 방식**으로 만듭니다 — 마우스로 이 창 저 창 옮겨다닐 필요가 없게.

### 화면 1 — 대기 상태 (조건 입력)

```
┌ 법령 조문 캡처 ──────────────────────────────────────┐
│                                                        │
│ ① 법령/고시명   [공동주택 하자의 조사, 보수…] [검색]     │
│                                                        │
│ ② 어느 시점?    ○ 특정 시점 하나   ● 기간 안의 모든 개정본│
│                 [2024-01-01] ~ [2026-12-31]            │
│                                                        │
│    ┌ 이 기간에 시행된 개정본 (오래된 순) ──────┐        │
│    │ ☑ 2025. 2. 3.   제2025-58호   (구)      │        │
│    │ ☑ 2026. 7. 8.   제2026-360호  (현행)    │        │
│    └──────────────────────────────────┘        │
│      기간을 넣으면 자동으로 체크됩니다.                  │
│      빼고 싶은 개정본은 체크를 끄시면 됩니다.             │
│      기간과 무관한 옛 개정본도 [연혁 전체 보기]로 추가 가능 │
│                                                        │
│ ③ 조문 번호     [1, 7, 10]                             │
│ ④ 빨간 밑줄 문구 [제1조(목적) 이 기준은            ]     │
│                                                        │
│ ⑤ 대상 한글 파일 [준비서면.hwp]         [찾아보기]       │
│ ⑥ 삽입 위치     ○ 플레이스홀더 자리  ● 문서 끝에 추가    │
│                                                        │
│         [＋ 이 조건을 목록에 담기]                      │
│                                                        │
│ ┌ 담은 목록 ────────────────────────────────┐        │
│ │ 제1조  개정본 2개 (2025.2.3. / 2026.7.8.)  [삭제] │        │
│ │ 제7조  개정본 2개                          [삭제] │        │
│ └────────────────────────────────────────┘        │
│   → 실제 만들어질 그림은 2조문 × 2개정본 = 4장         │
│                                                        │
│              [ 실행 ]                                  │
└────────────────────────────────────────────────────┘
```

- **한 번에 여러 조문**을 담아뒀다가 한 번에 처리하는 이유: 서면 하나에 조문 10~20개가 들어가는 게 보통이라, 매번 처음부터 다시 입력하면 지금 하시는 수작업이랑 크게 다르지 않기 때문입니다
- 잘못 담았으면 목록에서 그 줄만 `[삭제]` 누르면 됩니다
- **개정본 여러 개를 한 번에**: 기간을 넣으면 그 사이 시행된 개정본이 자동으로 체크되고, 체크를 끄거나 켜서 손으로 조정할 수 있습니다 (두 방식 모두 지원)
- 담은 목록 아래에 **총 몇 장이 만들어지는지** 미리 보여줍니다 — 실행 전에 예상 결과를 가늠하시라고

### 화면 2 — 실행 중 상태

```
┌ 법령 조문 캡처 ── 처리 중 ──────────────────────┐
│                                                    │
│   ▓▓▓▓▓▓▓▓░░░░░░░░░░  2 / 5 건 처리 중             │
│                                                    │
│   지금 하는 일: 제7조 PDF 다운로드 중…              │
│                                                    │
│  ┌ 진행 기록 ───────────────────────────┐        │
│  │ ✓ 제1조  버전 확인 → PDF → 밑줄 → 삽입 완료 │        │
│  │ ▶ 제7조  PDF 다운로드 중…                 │        │
│  └──────────────────────────────────┘        │
│                                                    │
│                    [ 중지 ]                        │
└────────────────────────────────────────────────┘
```

- 지금 몇 번째 조문의 어느 단계인지 **한 줄로 항상 보임** — 화면 까맣게 멈춘 것처럼 보이는 상황을 없애려는 목적
- **빨간 밑줄 문구를 PDF에서 못 찾으면**, 멈추지 않고 그 조문만 `✗ 실패 — 문구를 찾지 못함 (조문 개정으로 문구가 바뀌었을 수 있음)`으로 표시하고 나머지는 계속 진행. 실패한 것만 나중에 확인하시면 됨

### 화면 3 — 완료 상태

```
┌ 법령 조문 캡처 ── 완료 ────────────────────────┐
│                                                    │
│   5건 중 4건 완료, 1건 실패                        │
│                                                    │
│   ✗ 제10조 — "…" 문구를 PDF에서 찾지 못함           │
│                                                    │
│   [ 결과 한글 파일 열기 ]   [ 출력 폴더 열기 ]        │
│   [ 처음으로 ]                                     │
└────────────────────────────────────────────────┘
```

- 실패한 게 있으면 **어느 조문이 왜 실패했는지 문장으로** 알려드립니다 (에러 코드 같은 거 안 보여드림)
- "결과 한글 파일 열기"를 누르면 바로 한/글이 열려서 그 자리에서 확인 가능

### 디자인 원칙 (비개발자가 쓸 것을 전제로)

1. **전문용어를 화면에 안 씀** — "admRulSeq", "MST" 같은 내부 코드는 로그 파일에만 남기고 화면 문구는 전부 일상 말투
2. **실행 전 되돌아볼 기회를 줌** — 버전 자동 선택 결과를 실행 누르기 전에 미리 보여주는 것도 이 때문
3. **자동으로 멈추지 않음** — 조문 하나가 실패해도 전체를 멈추지 않고 계속 진행 (급한 서면 마감에 도움 되도록)
4. **되돌리기 쉬움** — 원본 한글 파일은 절대 직접 덮어쓰지 않고, 항상 새 파일(`_capture.hwp`)로 저장. 마음에 안 들면 원본은 그대로 있음

---

## 사전 검증 결과 (전부 실제 확인 완료)

계획의 전제가 되는 사실들은 추측이 아니라 브라우저·레지스트리·패키지를 직접 조사해 확인한 것입니다.

### law.go.kr 저장 흐름 — 두 번째 사진과 정확히 일치

| 항목 | 행정규칙(고시) | 법령(법률·시행령) |
|---|---|---|
| 본문 URL | `LSW/admRulLsInfoP.do?admRulSeq=…` | `LSW/lsInfoP.do?lsiSeq=…&efYd=YYYYMMDD` |
| 조문 체크박스 | `#Y0001000000` (조4+가지2+4자리) | `#Y000100` (조4+가지2) |
| 체크 시 뜨는 툴바 | `#joSelectLayer` — 저장/인쇄/복사/돋보기 (첫 번째 사진) | 동일 |
| 저장 아이콘 | `bdyCheckOutPut('0')` | 동일 |
| 저장 다이얼로그 | `#admRulOutPutLayer` | `#lsOutPutLayer` |
| 조문 범위 입력칸 | `#outputCustomJoNo` (체크한 조 자동 입력) | 동일 |
| PDF 라디오 | `#FileSavePdf` (`name=fileType`) | `#FileSavePdf1` (`name=fileType`) |
| 최종 저장 버튼 | `#aBtnOutPutSave` → `beforeSavePrint()` | **동일** |
| 탭 구성 | 범위설정 / 용지·폰트 설정, 법령본문·부칙·별표서식 | 동일 |

→ **어댑터 차이는 URL·체크박스 ID 자릿수·레이어 ID 세 가지뿐.** 나머지 로직은 공유됩니다.

### 시점(기준일) 기반 버전 선택

- **행정규칙**: `POST LSW/admRulHstListR.do` (body `admRulSeq=…`) → 연혁 전체 반환. 실측 결과 하자판정기준은 7개 버전:

  | 시행일 | 고시번호 | admRulSeq |
  |---|---|---|
  | 2026. 7. 8. | 제2026-360호 | `2100000282400` |
  | 2025. 2. 3. | 제2025-58호 | `2100000254050` ← 첫 번째 사진의 "구 기준" |
  | 2021. 12. 9. | 제2021-1262호 | `2100000206300` |
  | 2020. 11. 30. | 제2020-858호 | `2100000194959` |
  | 2016. 12. 30. | 제2016-1048호 | `2100000072850` |
  | 2015. 12. 17. | 제2015-951호 | `2200000043520` |
  | 2014. 1. 5. | 제2013-930호 | `2000000026604` |

  기준일 이하 최신 시행일 버전을 고르면 됩니다.
- **법령**: `lsInfoP.do?lsiSeq=…&efYd=YYYYMMDD` 파라미터가 그대로 먹힙니다 (직접 확인).

### 로컬 환경 (전부 설치 완료 — 추가 설치 불필요)

| 구성요소 | 버전 | 용도 |
|---|---|---|
| 한/글 | Office 2020, `HWPFrame.HwpObject` COM **등록됨** | 한글 문서 조작 |
| pyhwpx | 0.8.7 | `insert_picture` / `ShapeObjAttachCaption` / `move_caption('Top')` / `ShapeObjInsertCaptionNum` / `find` / `save_as` |
| PyMuPDF | 1.26.4 | PDF 빨간 밑줄·테두리 주석, 300dpi 영역 캡처 |
| Playwright | 1.61.0 (+chromium) | law.go.kr 저장 다이얼로그 구동 |
| Pillow | 10.1.0 | PNG 외곽 테두리·여백 |

---

## ⚠ 설계상 가장 중요한 제약

**korean-law MCP는 이 프로그램 안에서 호출할 수 없습니다.**
MCP 도구는 Claude 세션에서만 동작합니다. tkinter GUI는 독립 실행 파이썬 프로그램이므로 MCP에 접근할 수단이 없습니다.

→ **프로그램은 law.go.kr 엔드포인트를 직접 호출**합니다 (위 표의 URL들, 전부 인증 불필요·검증 완료).
→ korean-law MCP는 **개발·검증 단계에서 대조 확인용**으로만 씁니다 (조문 원문 텍스트가 맞는지 교차 검증).

추가로 확인된 MCP 한계: `legal_analysis mode=applicable_law`(시점법 판단)는 **법령 전용**이라 행정규칙에는 동작하지 않습니다. 그래서 위 연혁 API가 필수입니다.

---

## 아키텍처

새 폴더 `C:\Users\USER\Desktop\law_capture\` — court_monitor과 분리합니다 (공유 코드 없음).

**원칙: 파일마다 역할을 하나만 맡깁니다.** "화면을 그리는 코드"와 "실제로 일을 하는 코드"를 절대 같은 파일에 안 섞습니다. 이렇게 나눠두면 나중에 화면만 바꾸고 싶을 때(예: tkinter → 다른 방식) 안쪽 로직(법령 검색, PDF 처리, 한글 삽입)은 건드릴 필요가 없고, 반대로 "밑줄 긋는 방식을 바꾸고 싶다"고 하면 `annotate.py` 한 파일만 보면 됩니다.

```
law_capture/
  main.py           ← 실행 진입점. 정말 이것만 있음:
                       "화면 띄우고 → 이벤트 루프 시작" 세 줄 남짓
                       법령 검색·PDF·한글 삽입 코드는 단 한 줄도 없음

  config.py         상수만 모아둠: URL, 셀렉터, DPI, 여백, 캡션 형식, 본문 폭(mm)
                       → 사이트가 바뀌거나 수치를 조정할 때 이 파일만 봄

  core/             "기능" — 화면과 무관하게 그 자체로 동작·테스트 가능
    law_source.py     ① 검색 + 시점/기간 기준 개정본 해석
    law_pdf.py         ② Playwright로 조문 PDF 다운로드
    annotate.py        ③ PyMuPDF 빨간 밑줄 + 캡처 → PNG
    hwp_insert.py       ④ pyhwpx COM으로 한글 삽입 (크기·테두리·캡션)
    version_series.py    ⑤ 개정본 여러 개를 작업 단위로 펼치고 내용 동일 여부 판정
    pipeline.py        ①→⑤를 순서대로 실행하는 오케스트레이터
                          (진행 상황을 콜백으로 밖에 알려줌 — 화면을 직접 모름)

  ui/               "화면" — 순수하게 보여주기만, 판단은 안 함
    app_window.py      메인 창 뼈대(3가지 상태 전환), main.py가 이것만 가져다 씀
    input_view.py       화면 1 (조건 입력 + 목록 담기)
    progress_view.py    화면 2 (진행바 + 로그)
    result_view.py      화면 3 (완료 + 파일 열기)
    controller.py      "실행" 버튼이 눌리면 core/pipeline.py를 별도 스레드로 돌리고,
                          pipeline이 보내는 진행 상황을 화면 갱신 함수로 넘겨주는
                          연결 담당. UI 파일들은 이 파일을 통해서만 core/와 대화함
                          (input_view.py가 law_pdf.py를 직접 부르는 일이 없게 하는 장치)

  out/              PDF·PNG 중간 산출물 (조문별 폴더)
```

**세 층으로 나눈 이유** (건축으로 치면 "구조 / 설비 / 마감"을 따로 도면 그리는 것과 비슷합니다):

| 층 | 파일들 | 이 안에 들어가면 안 되는 것 |
|---|---|---|
| **core** (기능) | `law_source.py` ~ `pipeline.py` | tkinter import, "버튼", "화면" 같은 개념 일체 |
| **ui** (화면) | `app_window.py` ~ `result_view.py` | 법령 검색 로직, PDF 다운로드 코드, 한글 COM 코드 |
| **controller** | `controller.py` | 화면 그리기 코드(위젯 배치) — 오직 "연결"만 |
| **진입점** | `main.py` | 위 세 가지 전부. 정말 실행 세 줄만 |

이렇게 나눠두면 **`core/` 폴더 안의 파일들은 화면 없이도 커맨드라인에서 따로 테스트할 수 있습니다** — 아래 "진행 단계"의 1~4단계가 전부 `core/`만으로 검증 가능한 이유이기도 합니다. 화면(`ui/`)은 5단계에서 마지막에 얹습니다.

### ① `law_source.py` — 시점 기준 버전 해석

```python
@dataclass
class LawVersion:
    kind: str        # "admrul" | "law"
    seq: str         # admRulSeq 또는 lsiSeq
    name: str        # 공동주택 하자의 조사, 보수비용 산정 및 하자판정기준
    ef_date: date    # 2025-02-03
    promul: str      # 국토교통부고시 제2025-58호
    url: str
    is_current: bool # 기준일 버전이 현행이 아니면 캡션에 "구" 접두어
```

- `search_law_by_name(law_name)` → law.go.kr 검색 HTML 파싱 (법령·행정규칙 자동 폴백)
- `list_all_versions(law_name)` → 연혁 전체를 **오래된 시행일 순**으로 반환
  - 행정규칙 → `admRulHstListR.do` POST, `[시행 YYYY. M. D.]` 정규식 파싱
  - 법령 → 연혁 목록 조회 후 각 버전의 `efYd` 확보
- `find_version_effective_on(law_name, reference_date)` → 기준일 이하 최신 버전 1개 (단일 시점 모드)
- `find_versions_effective_between(law_name, period)` → 기간 안에 시행된 개정본 전부 (여러 개정본 모드)
- 현행이 아닌 버전은 `is_currently_in_effect=False` → 캡션 접두어 `구 ` 자동 부착 (첫 번째 사진의 `[그림 10] 구 공동주택 …` 형식)

### ② `law_pdf.py` — 조문 PDF 다운로드

Playwright(비headless 권장, 다이얼로그 안정성) 로 실제 UI 흐름을 그대로 재현합니다. `court_monitor/browser_utils.py`의 `launch_chromium` 자동 설치 재시도 패턴을 그대로 가져옵니다.

```python
async def download_article_pdf(version: LawVersion, jo_no: int, out_dir: Path) -> Path:
    # 1. version.url 접속
    # 2. 조문 체크박스 체크
    #    admrul: #Y{jo:04d}00{'0000'}   /  law: #Y{jo:04d}00
    #    (조 가지번호 '제6조의2' 는 value 파싱으로 대응)
    # 3. bdyCheckOutPut('0') 평가 → 저장 레이어 로드 대기
    # 4. #outputCustomJoNo 값이 조문번호로 채워졌는지 확인 (아니면 직접 입력)
    # 5. PDF 라디오 체크 (#FileSavePdf 또는 #FileSavePdf1)
    # 6. expect_download() 안에서 #aBtnOutPutSave 클릭
    # 7. download.save_as(out_dir / f"{name}_{ef}_제{jo}조.pdf")
```

- 저장 소요가 길 수 있으므로 다운로드 타임아웃 120초
- 체크박스 ID 규칙이 어긋나면 `input[name=joNoList]` 의 `value` 앞부분(`"1:0:…"` / `"1-0:0::…"`)으로 조 번호를 매칭하는 폴백

### ③ `annotate.py` — 빨간 밑줄 + 캡처

사용자 선택대로 **밑줄 위치는 텍스트 문자열로 직접 지정**합니다.

**밑줄은 PyMuPDF의 밑줄 주석 기능을 쓰지 않고 직접 선을 긋습니다.** 이유는 1단계 결과물 검토에서 드러난 두 가지 결함 때문이며, 아래 "1차 결과물 수정사항"에 실측 근거를 정리했습니다.

```python
def underline_and_capture_page(pdf_path: Path, phrases: list[str]) -> Path:
    document = fitz.open(pdf_path)
    page = document[0]

    for phrase in phrases:
        for text_area in find_text_areas(page, phrase):   # 줄바꿈되면 여러 개
            draw_underline_below_text(page, text_area)

    capture_area = find_content_area(page)                # 머리말·꼬리말 제외
    page.get_pixmap(dpi=CAPTURE_DPI, clip=capture_area).save(image_path)
    return add_black_border(image_path)


def draw_underline_below_text(page: fitz.Page, text_area: fitz.Rect) -> None:
    """글자 아랫끝보다 조금 더 아래에 빨간 선을 긋는다. (받침과 겹치지 않게)"""
    underline_height = text_area.y1 + UNDERLINE_GAP_BELOW_TEXT_IN_POINTS
    page.draw_line(
        fitz.Point(text_area.x0, underline_height),
        fitz.Point(text_area.x1, underline_height),
        color=RED,
        width=UNDERLINE_THICKNESS_IN_POINTS,
    )
```

- 문자열을 못 찾으면 **조용히 넘어가지 않고 `UnderlinePhraseNotFoundError`를 던져 화면에 표시** — 조문 개정으로 문구가 바뀐 경우를 놓치면 안 됩니다
- 띄어쓰기·줄바꿈 차이 대비: 공백 제거 후 정규화 매칭 폴백
- 캡처 영역은 페이지 위아래 가로 구분선을 찾아 그 사이만 남깁니다 — 머리말(문서명)과 꼬리말(`법제처 … 국가법령정보센터`)을 빼고 QR코드는 포함시키기 위함 (1단계에서 검증 완료)
- **그림에 테두리를 그려 넣지 않습니다.** 테두리는 한글 쪽에서 개체 속성으로 넣습니다 (④ 참조) — 사용자 요청에 따라 변경

### ④ `hwp_insert.py` — 한글 삽입

사용자 선택대로 **두 가지 모드 모두** 지원합니다.

```python
hwp = Hwp(visible=False, register_module=True)   # 보안모듈 자동 등록
hwp.open(target_hwp)
move_caret_to_insertion_point(hwp, job)          # 플레이스홀더 자리 또는 문서 끝

hwp.ParagraphShapeAlignCenter()                  # 그림 자체는 가운데
hwp.insert_picture(png, sizeoption=1, width=BODY_WIDTH_MM, height=calc_h)
apply_picture_border(hwp)                        # 한글 개체 테두리 0.12mm 실선
attach_caption_above_picture(hwp, caption_text)  # 아래 별도 구현 (pyhwpx 것 안 씀)

save_through_temporary_folder(hwp, out_hwp)      # 회사 보안 프로그램 우회
```

**그림 테두리는 한글 자체 기능으로 넣습니다 (0.12mm 실선).**

```python
def apply_picture_border(hwp) -> None:
    """
    방금 넣은 그림에 한글의 '개체 속성 > 선' 기능으로 테두리를 두른다.

    HAction.Run("ShapeObjDialog") 을 쓰면 실제로 대화상자가 화면에 떠서
    자동 실행이 멈춘다. 반면 GetDefault 로 현재 설정을 읽어와 값만 바꾼 뒤
    Execute 로 적용하면 대화상자 없이 조용히 반영된다.
    (pyhwpx 가 표 속성을 다룰 때 쓰는 것과 같은 방식)
    """
    hwp.find_ctrl()
    properties = hwp.HParameterSet.HShapeObject
    hwp.HAction.GetDefault("ShapeObjDialog", properties.HSet)
    properties.ShapeDrawLineAttr.style = hwp.hwp_line_type("Solid")
    properties.ShapeDrawLineAttr.Width = hwp.mili_to_hwp_unit(PICTURE_BORDER_WIDTH_IN_MILLIMETERS)
    properties.ShapeDrawLineAttr.Color = hwp.rgb_color(0, 0, 0)
    hwp.HAction.Execute("ShapeObjDialog", properties.HSet)
```

**검증 완료 (실물 확인).** 속성 이름은 문서에 없어 COM 객체를 직접 조회해 찾았습니다 — 선 설정은 `LineShape`가 아니라 **`ShapeDrawLineAttr`** 묶음 안에 있고, 항목은 `style`(선 종류), `Width`(두께), `Color`(색)입니다. 적용 전후 값이 `선종류 0→1, 두께 0→34`(0.12mm의 한글 내부 단위)로 바뀌는 것과, 테두리 없는 그림과 나란히 놓았을 때 아래쪽에만 선이 그려지는 것을 눈으로 확인했습니다. 팝업은 뜨지 않았습니다.

**캡션은 pyhwpx의 `move_caption`을 쓰지 않고 직접 구현합니다.** 그 함수가 캡션을 위로 올리면서 가운데 정렬을 강제하고, 게다가 문서 안의 캡션을 전부 훑기 때문입니다 (아래 "1차 결과물 수정사항" 참조).

```python
def attach_caption_above_picture(hwp, caption_text: str) -> None:
    """방금 넣은 그림에 캡션을 달고, 그림 위쪽에 왼쪽 정렬·휴먼명조 10pt로 배치한다."""
    hwp.find_ctrl()                       # 방금 넣은 그림 선택
    hwp.ShapeObjAttachCaption()           # 캡션 편집 상태로 진입
    hwp.set_font(FaceName=CAPTION_FONT_NAME, Height=CAPTION_FONT_SIZE_IN_POINTS)
    hwp.insert_text(caption_text)
    hwp.ParagraphShapeAlignLeft()         # 캡션만 왼쪽 정렬 (그림은 가운데 유지)
    move_caption_side(hwp, CaptionSide.TOP)
    hwp.Run("Cancel")                     # 캡션 편집 상태에서 빠져나옴
```

- **캡션 번호**: 기본은 한글 자동번호 필드(`ShapeObjInsertCaptionNum`) — 중간에 그림을 끼워도 뒷번호가 자동으로 밀립니다. `config.py`에서 고정 텍스트 번호로 전환 가능
- **크기**: A4 본문 폭(여백 제외 약 155mm)에 맞춰 원본 종횡비 유지 계산. `config.BODY_WIDTH_MM`로 조절
- **캡션 문구 자동 생성**: `{"구 " if not is_currently_in_effect}{법령명} {조문} 발췌` — 첫 번째 사진의 형식 그대로
- **저장은 반드시 임시 폴더 경유** (회사 보안 프로그램 차단 회피, 아래 중대 제약 항목 참조)

### ⑤ 여러 개정본을 한 번에 — `core/version_series.py`

**용도**: "2024~2026년 사이 제1조가 어떻게 바뀌었나"를 서면에 나란히 보여주는 경우. 조문 하나에 대해 개정본 수만큼 그림이 만들어집니다.

**작업 단위 구조**: 화면에서 담는 조건 한 줄(`ArticleCaptureJob`)이 개정본 수만큼의 실제 작업(`ArticleCaptureTask`)으로 펼쳐집니다.

```python
@dataclass(frozen=True)
class ArticleCaptureJob:
    """화면의 '담은 목록' 한 줄에 대응. 조문 1개 + 개정본 여러 개."""
    law_name: str
    target_versions: list[LawVersion]      # 오래된 시행일 순으로 정렬되어 들어옴
    article_number: int
    underline_phrases: list[str]
    target_hwp_path: Path
    insertion_mode: InsertionMode

@dataclass(frozen=True)
class ArticleCaptureTask:
    """실제로 그림 한 장을 만드는 단위. 개정본 1개에 대응."""
    version: LawVersion
    article_number: int
    underline_phrases: list[str]
    is_same_as_previous_version: bool      # 캡션 표시에 사용

def expand_into_tasks(job: ArticleCaptureJob) -> list[ArticleCaptureTask]: ...
```

**내용 동일 여부 판정**: 사용자 선택에 따라 **모든 개정본을 다 넣되, 앞 버전과 조문 내용이 같으면 캡션에 표시**합니다.

```python
def has_same_article_text(current_pdf: Path, previous_pdf: Path) -> bool:
    """
    두 개정본의 조문 본문이 실질적으로 같은지 비교한다.

    개정이 있었다고 해서 그 조문이 바뀐 것은 아니다. 다른 조문만 고치고
    해당 조문은 그대로 두는 개정이 흔하므로, 서면을 읽는 사람이
    '이건 안 바뀐 거구나'를 바로 알 수 있게 표시해 주기 위해 비교한다.

    비교 전에 머리말·시행일 줄을 걷어낸다. 이 부분은 개정 때마다 반드시
    바뀌므로 그대로 비교하면 항상 '다름'이 나와 판정이 무의미해진다.
    """
    return normalize(extract_article_body(current_pdf)) == \
           normalize(extract_article_body(previous_pdf))
```

- `extract_article_body`: 캡처 영역 계산과 같은 방식으로 머리말·꼬리말을 제외하고, 추가로 **제목·시행일·소관부처 줄까지 걷어낸** 순수 조문 본문만 추출
- `normalize`: 공백·줄바꿈 차이를 무시 (같은 내용이 다르게 줄바꿈되는 경우가 있음)

**캡션 형식**:

```
[그림 1] 구 공동주택 하자의 조사, 보수비용 산정 및 하자판정기준 제1조 발췌 (시행 2025. 2. 3.)
[그림 2] 공동주택 하자의 조사, 보수비용 산정 및 하자판정기준 제1조 발췌 (시행 2026. 7. 8.) (앞 버전과 동일)
```

- **시행일을 반드시 넣습니다** — 여러 장이 나란히 들어가면 시행일 없이는 구별이 안 됩니다
- 현행이 아닌 버전에만 `구 ` 접두어
- 앞 버전과 조문 내용이 같으면 `(앞 버전과 동일)` 꼬리표
- **배열 순서는 오래된 것부터** — 개정 흐름을 시간순으로 읽히게

### 화면 (`ui/` 폴더)

`court_monitor/gui.py`의 구조(작업 스레드 + 진행바 + 실시간 로그 + 시작/중지 + 완료 후 열기 버튼)를 그대로 따르되, 위에서 정한 대로 화면 그리기(`input_view.py`/`progress_view.py`/`result_view.py`)와 연결 로직(`controller.py`)을 나눠서 구현합니다.

```
┌ 법령 조문 캡처 ──────────────────────────────┐
│ 법령/고시명  [공동주택 하자의 조사, 보수비용…]  [검색] │
│ 기준일       [2025-06-01]  → 시행 2025.2.3. 제2025-58호 │
│ 조문         [1, 7, 10]                              │
│ 빨간 밑줄    [제1조(목적) 이 기준은                ]  │
│ 빨간 박스    [                                    ]  │
│ 대상 한글    [C:\…\준비서면.hwp]           [찾아보기] │
│ 삽입 위치    ○ 플레이스홀더 자리  ● 문서 끝에 추가    │
│ ─────────────────────────────────────────── │
│ [＋ 목록에 추가]     (여러 조문을 쌓아 일괄 처리)      │
│ ┌ 작업 목록 ───────────────────────────┐ │
│ │ 하자판정기준 2025.2.3. 제1조  밑줄 1건        │ │
│ │ 하자판정기준 2025.2.3. 제7조  밑줄 2건        │ │
│ └───────────────────────────────────┘ │
│ [실행]  [중지]      ▓▓▓▓▓░░░ 3/5                  │
│ 로그: 제1조 PDF 저장 완료 → 밑줄 1건 → 캡처 …      │
│ [결과 한글 열기]  [출력 폴더 열기]                    │
└─────────────────────────────────────────┘
```

- 기준일을 넣으면 **즉시 어느 버전이 선택됐는지 표시** — 시점 착오가 가장 위험하므로 실행 전에 눈으로 확인시킵니다
- 밑줄 문자열을 PDF에서 못 찾은 건은 로그에 빨간색으로 남기고 해당 그림은 건너뜀

---

## 리스크와 대응

| 리스크 | 대응 |
|---|---|
| ~~다운로드한 PDF에 텍스트 레이어가 없을 수 있음~~ | **해소됨 (1단계 검증 완료).** 470자 추출 성공, `search_for`로 밑줄 대상 문구 4개 모두 좌표 확보 |
| ~~pyhwpx `ShapeObjLineProperty`는 대화상자를 여는 액션~~ | **판단 정정.** `HAction.Run`만 대화상자가 뜨고, `GetDefault`+`Execute`는 조용히 적용됨. 한글 자체 테두리 기능(0.12mm) 사용 가능 — 2단계 착수 전 실물 확인 예정 |
| ~~한/글 COM 보안 승인 팝업~~ | **해소됨.** `Hwp(register_module=True)`로 팝업 없이 통과 확인 |
| **[중대] 회사 문서보안 프로그램이 한글의 로컬 저장을 차단함** | 아래 별도 항목 참조 |
| law.go.kr 화면 개편 시 셀렉터 파손 | 셀렉터를 전부 `config.py` 한 곳에 모으고, 실패 시 로그에 "사이트 구조가 바뀐 것 같습니다"를 명시 |
| 조 가지번호(제6조의2) 체크박스 ID 규칙 | `input[name=joNoList]`의 `value` 파싱 폴백으로 처리 (`"6-0:2::0006000002:…"`) |
| 다운로드 5분 이상 걸릴 수 있다는 사이트 경고 | 타임아웃 120초 + GUI에서 중지 가능 |

### 1차 결과물 수정사항 — 실제 산출물을 보고 확인된 5건

1단계에서 만든 `out/spike/spike_결과.hwp`를 검토한 결과 다섯 가지를 고쳐야 합니다. (1)~(3)은 결함이라 원인을 실측으로 특정했고, (4)~(5)는 서식 요구사항입니다.

#### (1) 빨간 밑줄이 한글 받침을 관통함

**실측 데이터** (`spike_제1조.pdf` 첫 페이지, `"제1조(목적) 이 기준은"`):

| 값 | 좌표 |
|---|---|
| 글자 크기 | 10.0pt |
| 글자 영역 윗변 | y = 176.50 |
| 기준선(baseline) | y = 184.50 |
| 글자 아랫끝(descender 반영) | y = 186.50 |
| **PyMuPDF 밑줄이 그어지는 위치** | **y ≈ 185.5** (글자 영역의 90% 지점) |

PyMuPDF의 `add_underline_annot`은 글자 영역 높이의 90% 지점에 선을 긋습니다. 이 위치가 **기준선(184.5)과 글자 아랫끝(186.5) 사이**에 떨어지는데, 한글은 받침(ㄱ, ㅈ, ㅁ 등)이 기준선 아래로 내려오므로 반드시 겹칩니다. 알파벳에는 문제가 없어 라이브러리 기본값이 이렇게 되어 있는 것입니다.

**해결**: 밑줄 주석 기능을 버리고 `page.draw_line()`으로 직접 긋습니다. 위치는 `글자 아랫끝 + 여유값`.

#### (2) 밑줄 두께를 지정할 수 없었음

1단계 실행 중 `Cannot set border for 'Underline'` 경고가 출력됐습니다. **PyMuPDF의 밑줄 주석은 두께 지정을 지원하지 않아** 기본 굵기(약 1pt)로 그려졌습니다.

**해결**: (1)과 동일 — `draw_line`은 `width` 인자를 그대로 받습니다.

#### (3) 캡션이 가운데 정렬됨

pyhwpx의 `move_caption()` 내부를 확인한 결과, 캡션 위치를 옮기면서 **`ParagraphShapeAlignCenter`를 무조건 실행**합니다. 게다가 이 함수는 문서 전체의 컨트롤을 순회하며 캡션(`UserDesc == "번호 넣기"`)을 **전부** 건드리므로, 그림을 여러 개 넣을 때 앞서 만든 캡션의 정렬까지 바꿔버립니다.

**해결**: `move_caption`을 쓰지 않고 캡션 부착·정렬·위치 설정을 직접 구현합니다 (위 `attach_caption_above_picture`). 방금 만든 캡션 하나만 대상으로 하므로 여러 장을 넣어도 서로 간섭하지 않습니다.

#### (4) 캡션 글씨체를 휴먼명조 10pt로

**폰트 존재 확인 완료**: `휴먼명조`는 한글 2020에 기본 탑재되어 있습니다.

```
C:\Program Files (x86)\Hnc\Office 2020\HOffice110\Shared\TTF\Hwp\HMKMM.TTF
→ TTF 이름 테이블 확인 결과 폰트명 '휴먼명조'
```

Windows 글꼴 목록(417개)에는 등록돼 있지 않지만 한글 내부에서는 정상 사용 가능합니다. pyhwpx의 `set_font(FaceName=..., Height=...)`는 한글·영문·기호 등 모든 문자 종류에 한 번에 적용해 주므로 이걸 씁니다.

#### (5) 그림 테두리를 한글 기능으로 0.12mm

**앞서 세운 계획을 뒤집습니다.** 계획 초안에서는 "한글의 테두리 설정은 대화상자가 떠서 자동 실행이 멈추므로 그림 파일에 직접 테두리를 그린다"고 했는데, pyhwpx 소스를 다시 확인한 결과 **구분이 있었습니다**:

| 호출 방식 | 결과 |
|---|---|
| `HAction.Run("ShapeObjDialog")` | 대화상자가 실제로 뜸 → 자동 실행 중단 (제가 우려한 것) |
| `HAction.GetDefault(...)` → 값 수정 → `HAction.Execute(...)` | **대화상자 없이 값만 적용** |

pyhwpx가 표 속성(`TablePropertyDialog`)을 다룰 때 이미 후자 방식을 쓰고 있습니다. 따라서 **한글 자체 테두리 기능을 쓸 수 있습니다.** ③단계의 PNG 테두리 그리기는 제거합니다.

#### 확정 설정값 (`config.py`)

```python
UNDERLINE_THICKNESS_IN_POINTS = 2.0        # 기존 약 1.0pt의 2배
UNDERLINE_GAP_BELOW_TEXT_IN_POINTS = 1.2   # 글자 아랫끝에서 이만큼 더 아래
CAPTION_ALIGNMENT = TextAlignment.LEFT     # 왼쪽 정렬
CAPTION_FONT_NAME = "휴먼명조"
CAPTION_FONT_SIZE_IN_POINTS = 10
PICTURE_BORDER_WIDTH_IN_MILLIMETERS = 0.12 # 한글 개체 속성으로 적용
```

**여백 계산 검증**: 다음 줄의 윗변은 y = 196.50이므로, 밑줄을 y = 187.70에 그어도 아래 줄까지 8.8pt 여유가 있습니다. 두께 2.0pt를 감안해도 겹치지 않습니다.

여섯 값 모두 `config.py`에 두어 결과물을 보고 조정할 수 있게 합니다.

#### 검증이 필요한 부분

`ShapeObjDialog` 액션 ID와 `LineShape` 속성명은 문서화가 부실해 실물 확인이 필요합니다. 2단계 착수 전에 짧게 시험하고, 만약 이 조합이 듣지 않으면 다음 순서로 대체합니다:

1. `hwp.CurSelectedCtrl.Properties` 파라미터셋에서 `LineShape` 하위 항목을 직접 수정
2. 그래도 안 되면 PNG에 테두리를 그리는 기존 방식으로 복귀 (0.12mm에 해당하는 픽셀 두께 환산) — 사용자에게 보고 후 결정

---

### [중대 제약] 회사 문서보안 프로그램의 한글 저장 차단 — 1단계에서 발견

**증상**: 한글 프로그램이 바탕화면·사용자 홈 폴더에 파일을 저장하려 하면 **오류 메시지 없이 조용히 실패**합니다. `SaveAs`는 성공했다고 응답하고 문서의 내부 경로도 바뀌지만, 디스크에는 파일이 만들어지지 않습니다.

**원인**: 이 컴퓨터는 회사 PC이고 문서보안 솔루션이 설치되어 있습니다. Windows Defender의 폴더 보호 기능은 꺼져 있으므로(`EnableControlledFolderAccess: 0`) 회사 보안 프로그램이 원인입니다.

**측정 결과**:

| 저장 위치 | 결과 |
|---|---|
| `Desktop\law_capture\out\` | 실패 (`save_as` 반환 False, 파일 없음) |
| 사용자 홈 (`C:\Users\USER\`) | 실패 |
| 임시 폴더 (`%TEMP%`) | **성공** (201,728바이트 정상 저장) |

**대응 — 임시 폴더 경유 저장**: 한글에게는 임시 폴더에 저장시키고, 파이썬이 최종 위치로 옮깁니다. 파이썬은 바탕화면 쓰기가 정상 동작하므로(PDF·PNG를 계속 쓰고 있음) 이 조합으로 우회됩니다. 보안 설정을 건드리지 않으므로 회사 정책 위반 소지도 없습니다.

```python
# core/hwp_insert.py 에 공통 적용
temporary_path = Path(tempfile.gettempdir()) / f"lawcapture_{uuid4().hex}.hwp"
hwp.save_as(str(temporary_path), format="HWP")
hwp.quit()                                  # 순서 중요: 한글이 파일을 붙잡고 있어
shutil.move(temporary_path, destination)    # 종료 전에는 옮길 수 없다
```

**주의 2가지** (1단계에서 실제로 부딪힌 것들):
1. **한글을 종료한 뒤에 옮겨야 함** — 저장 직후에는 한글이 파일을 잠그고 있어 `PermissionError`가 납니다
2. **한글이 만드는 모든 파일에 적용** — 문서 저장뿐 아니라 미리보기 이미지 생성(`create_page_image`)도 똑같이 차단되므로 동일하게 우회해야 합니다

---

## 진행 단계

**1단계 — 스파이크 (핵심 불확실성 제거).** 다른 코드 쓰기 전에 이것부터:
   - 하자판정기준 2025.2.3. 버전 제1조 PDF를 실제로 다운로드
   - `fitz.open(pdf)[0].get_text()` 로 텍스트 레이어 유무 확인
   - `page.search_for("제1조(목적)")` 이 rect를 반환하는지 확인
   - `Hwp(visible=False)` 인스턴스 생성 + 더미 PNG `insert_picture` + 캡션 부착이 팝업 없이 되는지 확인
   - **여기서 막히면 설계를 바꿔야 하므로 결과를 보고드리고 진행 여부를 다시 확인합니다**

**2단계** — `core/law_source.py` + `core/law_pdf.py` (행정규칙 어댑터). 하자판정기준 여러 시점·여러 조문으로 검증 — 화면 없이 커맨드라인에서
**3단계** — `core/annotate.py`. 밑줄이 받침과 겹치지 않는지, 두께가 적절한지 첫 번째 사진과 육안 비교
**4단계** — `core/hwp_insert.py`. 두 번째 사진과 육안 비교 (캡션 왼쪽 정렬·휴먼명조 10pt·테두리 0.12mm·크기). **착수 전에 한글 테두리 설정이 팝업 없이 먹히는지 짧게 확인** (위 "검증이 필요한 부분")
**5단계** — `core/version_series.py`. 2024~2026 구간 개정본 2개로 제1조를 뽑아, 내용 동일 판정과 캡션 표시가 맞는지 확인
**6단계** — `core/pipeline.py`로 ①~⑤ 연결 (여전히 화면 없음, 커맨드라인 인자로 실행)
**7단계** — `ui/` + `controller.py` + `main.py` — 화면을 마지막에 얹음. 이 시점엔 이미 핵심 기능이 전부 검증된 상태라 화면 쪽에서 생길 문제와 로직 쪽 문제가 섞이지 않음
**8단계** — 법령 어댑터(`lsInfoP.do`) 추가. 공동주택관리법 제39조로 검증

---

## 구현 중 확인된 사실 (계획과 달랐던 것들)

계획 단계에서 몰랐거나 잘못 알았던 것들입니다. 나중에 코드를 손볼 때 이 부분을 모르면 같은 함정에 다시 빠집니다.

### 1. 저장 창의 조문 목록을 반드시 다시 골라야 한다

본문에서 조문 체크박스를 켜도, 저장 창 안의 조문 목록은 **전부 켜진 채로** 열립니다.
이걸 놓치면 조문 하나를 요청했는데 22쪽짜리 전문이 받아집니다. 실제로 그랬습니다.

목록 값의 생김새가 갈래마다 다릅니다.

| 갈래 | 제1조를 가리키는 값 |
|---|---|
| 고시 | `0001-0000:00` |
| 법령 | `0001:00` |

또 이 목록은 파일 형식 라디오보다 **늦게 그려집니다.** 둘 다 기다려야 합니다.

### 2. PDF 를 받는 방식이 갈래마다 다르다

| 갈래 | 방식 |
|---|---|
| 고시 | 폼의 `submit()` 을 부른다 → 브라우저가 파일로 내려받음 |
| 법령 | `newLsSaveUpdate()` 를 부르고 `target="_self"` → **같은 창에서 PDF 를 열어버림** |

뒤쪽은 다운로드 이벤트가 아예 발생하지 않아 `expect_download` 로는 못 잡습니다.
그래서 **폼 전송을 가로채 주소와 값만 꺼낸 뒤 직접 요청**하는 방식으로 통일했습니다 (`SAVE_REQUEST_INTERCEPTOR_SCRIPT`).

추가로 법령 화면에는 `aBtnOutPutSave` 라는 같은 이름의 버튼이 **두 개**(숨어 있는 3단비교 창의 것 포함) 있어, 저장 창 안쪽으로 범위를 좁혀 찾아야 합니다.

### 3. 검색은 공개 API 가 가장 낫다

계획에서는 사이트 검색 화면을 파싱하려 했지만 세션에 묶여 있어 실패했습니다.
법제처 공개 API(`DRF/lawSearch.do`, `OC=test`)가 부분 이름 검색을 지원하고 XML 로 답해 훨씬 안정적입니다.

다만 공용 통로라 **간헐적으로 404** 가 납니다(실제로 겪음). 그래서 재시도 3회 + 한글 주소(`law.go.kr/행정규칙/이름`) 예비 경로를 넣었습니다.

법령 이력 조회는 `lsiSeq` 뿐 아니라 **`lsId` 도 함께** 줘야 응답합니다.

### 4. 법령은 같은 개정본이 여러 시행일로 중복 표시된다

한 번 개정할 때 조문마다 시행일을 다르게 두면(예: 대부분 3월, 일부 조문 6월) 사이트가 같은 문서를 여러 줄로 보여줍니다. 그대로 두면 같은 PDF 를 여러 번 받아 서면에 같은 그림이 중복됩니다. 문서 번호 기준으로 중복을 제거합니다.

또 검색이 부분 일치라 `주택법` 을 찾으면 `민간임대주택에 관한 특별법` 이 먼저 나옵니다. 이름이 정확히 같은 것을 앞으로 정렬해야 합니다.

### 5. 한글 관련 함정 셋

- **pyhwpx `set_font` 는 쓰면 안 됩니다.** 색상 기본값이 빈 문자열이라 호출하면 오류가 납니다. `HParameterSet.HCharShape` 를 직접 씁니다. 글자 종류(한글·영문·한자…) 마다 글꼴이 따로라 전부 지정해야 합니다.
- **넣은 그림을 반드시 지목해야 합니다.** `insert_picture` 가 돌려주는 개체를 들고 `set_pos_by_set(GetAnchorPos(0))` 로 다시 찾아야 합니다. 안 그러면 두 번째 그림의 캡션이 첫 번째 그림에 붙습니다.
- **캡션 편집 진입 확인이 필수입니다.** 캡션 안에서 '모두 선택 후 지우기' 를 하는데(자동 번호 제거), 진입에 실패한 상태로 실행하면 **본문이 통째로 지워집니다.** 실제로 그림이 사라지고 캡션 글자만 남는 일이 있었습니다. `get_pos()[0]` 이 본문(0)이면 중단합니다.
- 다음 그림을 넣기 전 `set_pos(0, 0, 0)` 으로 본문에 확실히 빠져나와야 합니다. 안 그러면 순서가 뒤집힙니다.

### 6. 한글 유령 프로세스

작업이 비정상 종료되면 화면에 안 보이는 한글이 남고, 다음 실행에서 **저장이 조용히 실패**합니다. 결과가 이상하면 `Hwp` 프로세스를 모두 종료하고 다시 실행해야 합니다. (README 에도 적어두었습니다)

### 7. 캡처 여백은 구분선을 넘으면 안 된다

여백을 그냥 더하면 머리말·꼬리말이 조금씩 딸려 들어옵니다. 법령은 구분선과 본문 사이가 좁아 실제로 머리말 글자가 잘린 채 그림 위에 걸쳤습니다. 여백을 주되 구분선에서 잘라냅니다.

---

## 남은 작업 순서 (2026-07-28 밤 갱신)

**끝난 것 (검증 완료):**
1. 우리 한글이 안 꺼지던 버그 (`_quit_hwp`)
2. 캡션 번호를 한글 자동번호로 (F)
3. '어느 시점?' 세 가지 (C)
4. 미리보기 위치·Ctrl+F (B)
5. 조문 여러 개·가지번호 (G·H) — `core/article_number.py` 신규
6. 드래그 순번 밑줄 (I) — `균열 [2번째]` 형태
7. 캡션·테두리 선택 (K) — `PictureInsertion`
8. 창 스크롤 (J)
9. 브라우저 창 숨김 (0-1-2) — `SHOW_BROWSER_WINDOW = False`

**다음에 손댈 것:** 당장 필수인 항목은 없음. 사용하면서 불편한 점이 생기면 그때 추가.

---

## [추가 기능] 조문 미리보기 + 드래그로 밑줄 지정

### Context

지금은 '빨간 밑줄' 칸에 문구를 **외워서 타이핑**해야 합니다. 조문에 정확히 뭐라고 쓰여 있는지 모르니 법령정보센터를 따로 열어 확인하고 옮겨 적는 일이 반복됩니다. 오타가 나면 실행이 실패하고, 그제서야 다시 확인하게 됩니다.

조문 내용을 화면에서 바로 보고 **드래그로 문구를 고르면** 이 왕복이 사라집니다.

### 먼저 해결해야 할 것 — 여러 줄에 걸친 문구

드래그하면 자연히 긴 문장을 고르게 됩니다. 그런데 **지금 코드는 그걸 처리하지 못합니다.**

공동주택관리법 제39조로 실측한 결과입니다.

| API 로 받은 문장 | PDF 에서 찾히는가 |
|---|---|
| `제39조(하자심사ㆍ분쟁조정위원회의 설치 등)` | 찾음 |
| `① 제36조부터 제38조까지에 따른 담보책임 및 …` | **못 찾음** |
| `② 하자분쟁조정위원회의 사무는 다음 각 호와 같다. …` | **못 찾음** |
| `1. 하자 여부 판정` | 찾음 |

9개 문장 중 **긴 5개가 전부 실패**했습니다. PDF 는 종이 폭에 맞춰 줄을 바꾸는데, `search_for` 는 한 줄 안에서만 찾기 때문입니다.

게다가 지금의 예비 수단(`_find_ignoring_whitespace`)은 문구를 못 찾으면 **그 줄 전체**에 밑줄을 긋습니다. 원하는 부분만 그어야 하는데 줄 끝까지 그어지는 셈입니다. 이것도 함께 고칩니다.

**해결 방법**: 문자 하나하나의 위치를 이용합니다.

```python
def find_text_areas(page: fitz.Page, phrase: str) -> list[fitz.Rect]:
    """
    문구가 놓인 자리들을 찾는다. 여러 줄에 걸쳐 있어도 줄마다 정확한 구간만 돌려준다.
    """
    exact_matches = page.search_for(phrase)   # 한 줄 안에 있으면 이걸로 끝난다
    if exact_matches:
        return exact_matches
    return _find_across_lines(page, phrase)
```

`_find_across_lines` 의 방식:

1. `page.get_text("rawdict")` 로 **글자 하나마다의 자리(bbox)** 를 모읍니다
2. 공백을 뺀 글자들을 페이지 순서대로 이어 하나의 문자열로 만들고, 각 글자가 원래 몇 번째 줄의 어디였는지 기억합니다
3. 찾을 문구도 공백을 빼고, 그 문자열 안에서 위치를 찾습니다
4. 걸치는 글자들을 **줄별로 묶어** 줄마다 `왼쪽끝 ~ 오른쪽끝` 사각형을 만듭니다

이러면 세 줄에 걸친 문장은 밑줄 세 개가 각 줄의 정확한 구간에만 그어집니다. 띄어쓰기 차이도 자연히 무시됩니다.

### 0. [최우선] 화면에서 실행하면 한글이 아예 안 열리는 문제

**증상**: 화면에서 `[실행]` 을 누르면 이렇게 실패합니다.

```
× 한글 문서 작업 중 '한글 프로그램 실행' 에 실패했습니다.
  ((-2147221008, 'CoInitialize가 호출되지 않았습니다.', None, None))
```

**원인**: 법령이나 날짜와는 무관합니다. (실제로 `주택법 2011-01-28` 은 시행 2011. 1. 28. 판으로 정확히 찾아집니다)

윈도우에서 한글 같은 프로그램을 코드로 조작하려면 **그 일을 하는 흐름마다 준비 절차(CoInitialize)를 한 번씩 거쳐야** 합니다. 그런데 화면은 창이 멈추지 않도록 실제 작업을 **별도 흐름**에서 돌립니다. 그 흐름에서 준비 절차를 밟지 않아 한글을 붙잡지 못하는 것입니다.

**왜 여태 못 찾았나**: 지금까지의 검증은 두 갈래였는데 둘 다 이 길을 지나지 않았습니다.

| 검증 방법 | 어느 흐름에서 도는가 | 이 문제를 만나는가 |
|---|---|---|
| 명령줄 실행 (`run_from_command_line.py`) | 하나뿐인 주 흐름 | 안 만남 (준비 절차가 저절로 됨) |
| 화면 검증 (`check_gui.py`) | 버튼 연결·화면 전환만 확인, 실제 실행은 안 함 | 안 만남 |

**즉 화면에서 [실행] 을 눌러 끝까지 가본 적이 한 번도 없었습니다.** 뼈아픈 구멍입니다.

**고칠 곳**: `core/hwp_insert.py` 의 `HwpDocumentEditor` — 한글을 잡기 직전에 준비 절차를 밟고, 끝나면 정리합니다. 한글을 실제로 쓰는 파일에 두는 것이 맞습니다 (다른 곳에서 이 파일을 가져다 써도 저절로 처리되므로).

```python
def __enter__(self):
    # 윈도우에서 다른 프로그램을 조작하려면 흐름마다 이 준비를 거쳐야 한다.
    # 화면은 창이 멈추지 않도록 별도 흐름에서 작업을 돌리는데,
    # 그 흐름에서 이것을 빠뜨리면 한글을 아예 잡지 못한다.
    self._prepare_com_for_this_thread()
    ...
```

이미 준비된 흐름에서 또 불러도 문제가 없도록(명령줄 실행이 그렇습니다) 예외를 삼키고 넘어갑니다.

**검증 방법도 함께 고칩니다.** 지금의 화면 검증은 실제 실행을 안 해서 이 문제를 놓쳤습니다. **화면에서 [실행] 을 눌러 한글 파일이 실제로 만들어지는 것까지** 확인하는 검증을 추가합니다. 그렇지 않으면 같은 종류의 문제를 또 놓칩니다.

### 0-1. [최우선] 사용자가 쓰던 한글 문서를 닫아버리는 문제

**사용자가 일하던 한글 창이 프로그램 때문에 닫혔습니다.** 원인이 두 가지인데, 둘 다 고쳐야 합니다.

#### (가) 개발 중에 한글 프로세스를 강제로 종료한 것

작업을 검증하면서 남아 있는 한글을 정리하려고 `Hwp` 프로세스를 **전부** 강제 종료하는 명령을 여러 번 썼습니다. 사용자가 쓰던 문서까지 함께 닫혔습니다.

**앞으로 하지 않습니다.** 검증할 때도 프로세스를 건드리지 않고, 우리가 만든 것만 정리합니다.

#### (나) 프로그램 자체가 사용자의 한글을 붙잡는 것 — 이쪽이 더 심각

`pyhwpx` 로 한글을 잡을 때 **이미 떠 있는 한글에 붙는 경우**가 있습니다. 실제로 진단해 보니 `Hwp(new=True)` 로 잡았는데도 `열린 문서 수: 2` 가 나왔습니다. 남의 문서가 열려 있는 인스턴스에 올라탄 것입니다.

그 상태에서 우리 코드는 마지막에 이렇게 합니다.

```python
def quit(self):
    self.hwp.Quit()      # <- 한글 '프로그램 전체' 를 종료한다
```

**즉 프로그램을 한 번 돌릴 때마다 사용자가 열어둔 한글이 통째로 닫힐 수 있습니다.** 개발 중에만 생기는 일이 아니라 평소 사용 중에도 생깁니다.

**고치는 방법 — 우리 한글 창을 따로 열고, 거기서만 일하고, 끝나면 그것만 닫는다.**

원래 이렇게 되어야 했고, 지금 코드도 대체로 그 구조입니다. 다만 세 군데가 어긋나 있었습니다.

| 어긋난 곳 | 증상 | 고칠 것 |
|---|---|---|
| 끝날 때 한글 **전체**를 종료 | 사용자 문서가 닫힘 | 우리가 켠 인스턴스일 때만 종료 |
| '한글이 이미 떠 있으면 종료 안 함' 으로 판단 | **우리 것까지 안 꺼져서** 보이지 않는 한글이 실행할 때마다 쌓임 | 켜기 전후 목록을 견주어 **'우리가 켠 것' 을 정확히 구분** |
| 새 인스턴스에 문서를 **또** 추가 | `빈 문서 1` 과 `빈 문서 2` 가 같이 뜸 | 새로 켠 인스턴스에 딸려 오는 **빈 문서를 그냥 씀** |

마지막 것이 사용자가 본 '빈 문서 1, 2' 의 정체입니다. 한글을 새로 켜면 `빈 문서 1` 이 딸려 오는데, 거기에 `Add()` 로 하나를 더 만들어 `빈 문서 2` 가 생긴 것입니다. 우리 인스턴스는 어차피 우리만 쓰므로 딸려 온 문서를 그대로 쓰면 됩니다.

**정리된 흐름** — 사용자가 말한 그대로입니다.

```
우리 한글 창 열기 (새 인스턴스)
      ↓
딸려 온 빈 문서에 작업 (그림·테두리·캡션)
      ↓
임시 폴더에 저장  (회사 보안 프로그램이 바탕화면 저장을 막으므로)
      ↓
문서 닫고 한글 종료
      ↓
파이썬이 결과 파일을 제자리로 옮김
```

`Add()` 와 `SetActive_XHwpDocument()` 는 **혹시 우리 창이 아니라 남의 한글에 붙었을 때만** 쓰는 안전장치로 남깁니다. 그 경우에는 우리 문서를 따로 만들어 쓰고, 한글은 절대 끄지 않습니다.

이 구조면 사용자가 한글로 다른 일을 하는 중에도 프로그램을 돌릴 수 있고, 보이지 않는 한글이 쌓이지도 않습니다.

#### (다) 안내 문구도 고칩니다

`README.md` 에 "결과가 이상하면 작업 관리자에서 `Hwp` 프로세스를 모두 종료하세요" 라고 적어두었는데, 이건 **사용자의 작업을 날리라는 말**이 됩니다. (나) 를 고치면 그럴 필요도 없어지므로 이 안내를 지웁니다.

#### (라) 검증 중 확인된 남은 두 가지 (2026-07-28) — **이후 수정 완료**

격리 자체는 **확인되었습니다.** 사용자 역할 한글을 띄워둔 채 우리 작업을 돌린 결과:

| 확인 항목 | 결과 |
|---|---|
| 빈 문서를 하나만 쓰는가 | **통과** (`빈 문서 2` 안 생김) |
| 사용자 문서가 살아 있는가 | **통과** |
| 사용자 문서 내용이 그대로인가 | **통과** (`건드리면 안 되는 내용` 그대로) |
| 결과 파일이 만들어졌는가 | **통과** |
| **우리가 켠 한글이 정리됐는가** | **통과** (문서 선닫기 없이 바로 quit + 잔존 확인) |

**수정 내용:** `_quit_hwp` 에서 우리 인스턴스면 문서를 따로 닫지 않고 바로 종료한다.
종료 후 이름표로 남았는지 확인하고, 남으면 한 번 더 시도한다.
`check_leaves_user_hwp_alone.py` 의 `close_quietly` 도 임시 저장 후 바로 quit 하도록 맞춤.

### 0-1-2. 브라우저를 화면에 띄우지 않기

지금은 법령을 받아올 때 **크롬 창이 화면에 뜹니다** (`SHOW_BROWSER_WINDOW = True`). 창이 튀어나와 하던 일을 가리므로 성가십니다.

처음에 이렇게 둔 이유는 "저장 창이 화면에 그려져야 동작할지 모른다" 는 염려 때문이었는데, **지금은 그 염려가 사라졌습니다.** 저장 방식을 바꿔서, 사이트가 보내려는 요청을 가로채 직접 받아오기 때문입니다. 화면에 그리는 것과 무관합니다.

- 기본을 **화면에 안 보이게(headless)** 로 바꿉니다
- 다만 **먼저 확인하고 바꿉니다.** 안 보이는 상태에서도 조문 PDF 가 제대로 받아지는지 시험한 뒤에 기본값을 바꿉니다. 안 되면 지금처럼 두되 창을 최소화하는 쪽을 찾습니다
- `config.py` 의 값 하나만 바꾸면 다시 보이게 할 수 있게 남겨둡니다 (문제가 생겼을 때 눈으로 봐야 하므로)

### 0-2. [최우선] 긴 조문의 뒷부분이 조용히 잘려 나가는 문제

**증상**: 조문이 길면 PDF 가 여러 쪽으로 나옵니다. 그런데 지금 코드는 **첫 쪽만** 그림으로 만듭니다 (`document[0]`). 나머지 쪽은 아무 말 없이 버려집니다.

**얼마나 흔한가** — 이미 만들어진 PDF 들을 세어 보니 3쪽짜리가 여럿입니다.

| 파일 | 쪽수 | 글자 수 |
|---|---|---|
| `건축법_2012-07-22_제2조.pdf` | **3쪽** | 3,582자 |
| `주택법_2012-04-15_제2조.pdf` | **3쪽** | 4,489자 |
| `…PC 301)_2011-11-24_제4조.pdf` | **3쪽** | 4,301자 |
| `하자판정기준_2025-02-03_제1조.pdf` | 1쪽 | 471자 |

「주택법」제2조(정의)는 3,084자 48줄입니다. 정의 조항·별표를 낀 조문은 대개 이렇습니다. **드문 경우가 아니라 흔한 경우입니다.**

**세 곳에서 문제가 됩니다**:

1. **그림이 잘림** — 조문 뒷부분이 서면에 안 들어갑니다. 더 나쁜 것은 **아무 경고도 없어서** 잘린 줄 모른다는 점입니다
2. **밑줄을 못 찾음** — 밑줄 칠 문구가 둘째 쪽에 있으면 `문구를 찾지 못했습니다` 로 실패합니다. 분명히 조문에 있는 문구인데 없다고 하니 원인을 짐작하기 어렵습니다
3. **개정본 비교가 틀림** — 앞 판과 같은지 비교할 때도 첫 쪽만 봅니다. 뒷부분만 개정된 조문은 `(앞 버전과 동일)` 로 잘못 표시됩니다

**고치는 방법** — 쪽마다 그림 하나씩 만들어 차례로 넣고, 캡션에 몇 번째 쪽인지 적습니다.

```
[그림 1] 주택법 제2조 발췌 (시행 2011. 1. 28.) (1/3)
[그림 2] 주택법 제2조 발췌 (시행 2011. 1. 28.) (2/3)
[그림 3] 주택법 제2조 발췌 (시행 2011. 1. 28.) (3/3)
```

한 쪽뿐이면 `(1/1)` 을 붙이지 않습니다. 지금과 똑같이 나옵니다.

**고칠 곳** (`core/annotate.py` 세 함수):

| 함수 | 지금 | 바꿀 것 |
|---|---|---|
| `underline_and_capture` | 첫 쪽만 그림 하나 | 쪽마다 그림 하나, 목록으로 돌려줌 |
| 밑줄 문구 찾기 | 첫 쪽에서만 | 모든 쪽에서. **어느 쪽에도 없을 때만** 실패 |
| `extract_article_body_text` | 첫 쪽만 | 모든 쪽을 이어붙임 (개정본 비교용) |

`core/pipeline.py` 는 그림 하나가 아니라 여러 장을 받아 차례로 넣도록 바꿉니다. 이 구조는 뒤의 '조문 여러 개' (G) 에서도 그대로 씁니다 — 여러 조문을 담아 여러 쪽이 되는 것도 같은 일이기 때문입니다.

### A. 조문 텍스트 빠르게 가져오기 — `core/article_text.py` (신규)

미리보기에 PDF 는 필요 없습니다. **글자만 있으면 됩니다.** 법제처 공개 API 로 1~2초면 받아옵니다 (브라우저를 띄우지 않습니다).

| 갈래 | 요청 | 응답 |
|---|---|---|
| 법령 | `lawService.do?target=law&MST={판번호}&JO={조문6자리}` | 그 조문만 (약 3KB) |
| 행정규칙 | `lawService.do?target=admrul&ID={판번호}` | 문서 전체 (약 110KB) |

- **법령**은 `조문내용`·`항내용`·`호내용`·`목내용` 태그가 **문서 순서대로** 나옵니다. 순서대로 이어붙이면 됩니다 (실측 확인).
- **행정규칙**은 항·호 구분 없이 전부 `조문내용` 태그입니다. `제N조` 로 시작하는 항목부터 다음 `제M조` 직전까지 모아야 합니다.

```python
def fetch_article_text(version: LawVersion, article_number: int) -> str:
    """조문 내용을 글자로 받아온다. 미리보기용이라 빠른 것이 중요하다."""
```

요청 통로는 기존 `core/law_site.py` 의 재시도·예비 경로를 그대로 씁니다.

### B. 미리보기 패널 — `ui/preview_view.py` (신규)

**놓는 자리: '빨간 밑줄' 칸 바로 아래.** 창 맨 아래가 아닙니다.

드래그한 문구가 바로 위 칸에 들어가는 것이 눈에 보여야 하기 때문입니다. 미리보기가 창 맨 아래에 있으면 드래그한 뒤 시선을 한참 위로 올려야 제대로 들어갔는지 확인할 수 있습니다.

```
① 법령/고시명    [하자판정기준]              [검색]
                 [공동주택 하자의 조사, …  ▼]
② 어느 시점?     ● 기간 안의 모든 개정본
    ☑ 전체 선택 (2개)
    ☑ 시행 2025. 2. 3. …
③ 조문 번호      [7]  [미리보기]
④ 빨간 밑줄      [균열 폭이 0.3mm 이상인 경우          ]
  ┌ 조문 미리보기 ──────────────────────┐   <- 바로 여기
  │ 어느 판: [시행 2025. 2. 3. ▼]   제7조    │
  │ 제7조(콘크리트 균열) ① 콘크리트에 발생한 …│
  └──────────────────────────────────┘
⑤ 대상 한글 파일  […]                [찾아보기]
   담은 목록 …
   [ 실행 ]
```

화면 세로가 길어지므로 창 크기도 함께 키웁니다 (`ui/app_window.py` 의 `WINDOW_SIZE`). 그리고 남는 공간은 **미리보기가 가져가고** 담은 목록은 고정 높이로 둡니다 — 읽어야 하는 것은 조문이고, 담은 목록은 몇 줄이면 충분하기 때문입니다.

고칠 곳: `ui/input_view.py` 에서 미리보기 패널을 붙이는 순서를 `_build_article_rows()` 바로 뒤로 옮깁니다 (tkinter 는 붙이는 순서가 곧 위아래 순서입니다).

#### 미리보기 안에서 찾기 (Ctrl+F)

조문이 길면(제7조만 해도 여러 항·호가 이어집니다) 눈으로 훑어 찾기가 번거롭습니다. 웹브라우저나 한글에서 쓰는 것과 같은 방식으로 찾을 수 있게 합니다.

```
┌ 조문 미리보기 ────────────────────────────────┐
│ 어느 판: [시행 2025. 2. 3. ▼]   제7조            │
│ ┌ 찾기 ─────────────────────────────────┐ │   <- Ctrl+F 로 나타남
│ │ [균열          ]  3개 중 2번째   [◀] [▶] [X] │ │
│ └───────────────────────────────────┘ │
│ ┌───────────────────────────────────┐ │
│ │ 제7조(콘크리트 균열) ① 콘크리트에 발생한   │ │
│ │ ▓균열▓은 균열 폭이 0.3mm 이상인 경우 …    │ │
│ └───────────────────────────────────┘ │
│ 밑줄 칠 부분을 마우스로 드래그하세요.              │
└──────────────────────────────────────────┘
```

동작:

| 조작 | 결과 |
|---|---|
| `Ctrl+F` | 찾기 칸이 나타나고 커서가 그리로 감 |
| 글자 입력 | 입력하는 즉시 맞는 곳이 전부 옅은 노란색으로 표시되고 `3개 중 1번째` 처럼 개수가 나옴 |
| `Enter` / `[▶]` | 다음 곳으로 이동. 그 자리는 진한 색으로 구분되고 화면이 따라 스크롤됨 |
| `Shift+Enter` / `[◀]` | 이전 곳으로 이동 |
| `Esc` / `[X]` | 찾기 칸이 닫히고 표시가 지워짐 |

- 마지막에서 `Enter` 를 또 누르면 처음으로 돌아갑니다 (브라우저와 같은 방식)
- 못 찾으면 `찾는 글자가 없습니다` 를 조용히 보여줍니다. 팝업은 띄우지 않습니다
- **찾기 표시와 '고른 부분' 표시는 색이 다릅니다** — 찾는 중인 곳은 노란색, 밑줄로 고른 곳은 옅은 빨간색이라 헷갈리지 않습니다
- 찾은 뒤에도 드래그는 그대로 됩니다. 찾아서 위치를 확인하고 그 자리를 드래그하는 흐름입니다

구현은 `tk.Text` 가 이미 갖고 있는 `search()` 와 태그 기능을 씁니다. 별도 라이브러리가 필요 없습니다.

```
┌ 조문 미리보기 ─────────────────────────────────┐
│ 시행 2025. 2. 3. 판  ▼      [실제 모습 보기]      │
│ ┌──────────────────────────────────────┐ │
│ │ 제7조(콘크리트 균열) ① 콘크리트에 발생한 균열은  │ │
│ │ 균열 폭이 0.3mm 이상인 경우 시공하자로 본다.    │ │
│ │ ② 제1항에도 불구하고 다음 각 호의 …            │ │
│ └──────────────────────────────────────┘ │
│ 밑줄 칠 부분을 마우스로 드래그하세요.               │
└────────────────────────────────────────────┘
```

- 읽기 전용 `tk.Text` 에 조문을 넣고 드래그 선택을 허용합니다
- **드래그를 뗀 순간** 선택한 글자가 밑줄 칸에 들어갑니다. 여러 번 드래그하면 `;` 로 이어집니다
- 방금 넣은 부분은 미리보기에서도 옅은 빨간색으로 표시해 무엇을 골랐는지 보이게 합니다
- 개정본을 여러 개 골랐으면 **어느 판을 볼지 고르는 드롭다운**을 둡니다 (기본은 가장 오래된 판)
- `[실제 모습 보기]` 를 누르면 그때만 브라우저로 PDF 를 받아 그림으로 보여줍니다 (10~15초). 결과물과 같은 모습을 확인하고 싶을 때만 씁니다

### C. '어느 시점?' 을 세 가지로 정리

**지금의 문제**: '특정 시점 하나' 를 골라도 날짜 칸이 `[시작] ~ [끝]` 두 개 그대로 남아 있고, 프로그램은 **뒤쪽 칸만 몰래 씁니다.** 앞 칸에 날짜를 넣으면 무시되고, 왜 안 되는지 알 길이 없습니다. 그리고 '제정부터 지금까지 전부' 를 고를 방법이 없습니다.

**속의 계산은 이미 맞습니다.** 「지능형 홈네트워크 설비 설치 및 기술기준」에 `2012-09-20` 을 주면 정확히 **시행 2011. 3. 4.** 판을 골라냅니다 (다음 개정이 2013. 4. 12. 이므로). 고칠 것은 화면입니다.

**세 가지 방식으로 정리하고, 고른 것에 맞는 칸만 보여줍니다.**

```
① 특정 시점 하나    기준일 [2012-09-20]            [그 시점 판 찾기]
                          ↑ 칸 하나만

② 기간 안의 개정본  [2024-01-01] ~ [2026-12-31]    [개정본 찾기]

③ 전체 기간        (날짜 칸 없음)                  [전체 개정본 찾기]
                   제정부터 현행까지 모두
```

- 방식을 바꾸면 **필요 없는 칸은 숨깁니다.** 지금처럼 안 쓰는 칸이 남아 헷갈리는 일이 없게 합니다
- 단일 모드는 **첫 칸** 을 씁니다 (읽는 순서대로). `기준일` 이름표를 붙입니다
- 버튼 글자도 방식에 맞춰 바뀝니다

**'전체 기간' 은 개정본이 아주 많을 수 있습니다.** 실제로 「주택법」은 **172개**입니다. 그대로 두면 그림이 172장 만들어집니다.

그래서 이렇게 다룹니다.

- 개정본 목록에 **자체 스크롤**을 둡니다 (높이를 정해두고 그 안에서 굴러가게). 172줄이 창을 밀어내지 않도록
- 개수가 많으면 목록 위에 눈에 띄게 알립니다
  ```
  개정본이 172개입니다. 필요한 것만 남기고 체크를 꺼주세요.
  ```
- 이때 **처음부터 전부 꺼진 상태**로 둡니다. 172장을 실수로 만드는 것보다 필요한 것을 켜는 편이 안전합니다 (기간·단일 모드는 지금처럼 전부 켜진 채로 둡니다 — 몇 개 안 되므로)
- 앞서 만든 `[전체 선택]` 체크박스로 한 번에 켜고 끌 수 있습니다

고칠 곳: `ui/input_view.py` 의 `_build_period_row`(방식에 따라 칸 숨기기·보이기), `_collect_versions_for_current_mode`(전체 기간이면 `controller.list_versions` 를 부름 — 이미 있는 함수입니다), `_build_version_list`(스크롤 되는 목록으로).

### D. 개정본 목록에 전체 선택/해제

개정본이 많을 때(공동주택관리법은 기간에 따라 8개까지 나옵니다) 하나씩 끄고 켜는 것이 번거롭습니다.

목록 맨 위에 **전체 선택 체크박스**를 둡니다.

```
┌ 캡처할 개정본 (오래된 순) ────────────────────┐
│ ☑ 전체 선택 (4개)          <- 새로 추가         │
│ ─────────────────────────────────────  │
│ ☑ 시행 2025. 1. 1.   법률 제20385호  (구)     │
│ ☑ 시행 2025. 10. 1.  법률 제21065호  (구)     │
│ ☐ 시행 2026. 3. 3.   법률 제21166호  (구)     │
│ ☑ 시행 2026. 7. 1.   법률 제21447호  (현행)   │
└──────────────────────────────────────┘
```

- 전체 선택을 켜면 전부 체크, 끄면 전부 해제
- 개별 항목을 건드리면 전체 선택 표시도 따라 갱신 (하나라도 꺼져 있으면 전체 선택이 꺼진 모습)
- 옆에 `(4개)` 처럼 현재 선택된 개수를 보여줘 실행 전에 눈으로 확인 가능

### E. 캡션에서 '구' 빼기

지금은 현행이 아닌 판이면 캡션 앞에 `구 ` 를 붙입니다.

```
(지금)   [그림 1] 구 공동주택 하자의 조사, 보수비용 산정 및 하자판정기준 제1조 발췌 (시행 2025. 2. 3.)
(바꿀 것) [그림 1] 공동주택 하자의 조사, 보수비용 산정 및 하자판정기준 제1조 발췌 (시행 2025. 2. 3.)
```

캡션에 이미 시행일이 들어가므로 어느 판인지는 그것으로 충분합니다.

**화면의 `(구)` / `(현행)` 표시는 그대로 둡니다.** 개정본을 고를 때는 어느 것이 현행인지 한눈에 보여야 하기 때문입니다. 문서에 들어가는 문구에서만 뺍니다.

고칠 곳: `config.CAPTION_TEMPLATE` 에서 `{former_prefix}` 를 없애고, `core/version_series.py` 의 `build_caption` 에서 그 값을 넘기지 않습니다. `is_currently_in_effect` 자체는 화면 표시에 계속 쓰이므로 남겨둡니다.

### F. 캡션 번호를 한글 자동번호로

**지금의 문제**: 캡션의 `[그림 1]` 에서 숫자가 그냥 타이핑된 글자입니다. 파이썬이 세어서 적어 넣습니다.

그래서 이런 일이 생깁니다.

| 상황 | 지금 | 한글 자동번호를 쓰면 |
|---|---|---|
| 이미 그림이 3개 있는 준비서면에 넣을 때 | 새로 넣은 것이 `[그림 1]` 부터 시작 → 번호 중복 | `[그림 4]` 부터 이어짐 |
| 나중에 문서 중간에 그림을 끼워 넣을 때 | 뒷번호가 전부 어긋남. 손으로 다시 고쳐야 함 | 뒷번호가 저절로 밀림 |
| 그림 하나를 지웠을 때 | 번호에 구멍이 남음 | 저절로 다시 매겨짐 |

**한글에는 이미 이 기능이 있습니다.** 캡션 안에서 `Ctrl+N,C` 로 넣는 '번호 넣기' 가 그것이고, 자동으로 갱신되는 필드입니다.

**이미 확인된 사실**: 첫 스파이크에서 한글이 캡션에 자동으로 넣어준 번호가 `그림 1`, `그림 2` 였는데, 같은 자리에 제가 적어 넣은 번호는 `[그림 2]`, `[그림 1]` 로 **순서가 반대**였습니다. 한글이 문서에 놓인 순서대로 스스로 다시 매겼다는 뜻이고, 곧 자동 갱신 필드라는 증거입니다.

**바꿀 방식** — 지금은 한글이 넣어준 번호를 지우고 우리 글자로 덮어쓰고 있습니다. 이걸 그만두고, 번호는 한글 것을 그대로 두고 앞뒤에 대괄호와 설명만 붙입니다.

```python
def _attach_caption(self, picture, description: str) -> None:
    """
    그림에 캡션을 단다. 번호는 한글이 스스로 매기게 둔다.

    캡션을 달면 한글이 '그림 1' 같은 번호를 자동으로 넣어준다.
    이것은 단순한 글자가 아니라 문서에 놓인 순서를 따라가는 '필드' 라서,
    나중에 그림을 중간에 끼우거나 지워도 번호가 저절로 다시 매겨진다.
    그래서 우리가 숫자를 세어 적어 넣지 않는다.
    """
    self._select_picture(picture)
    self._hwp.ShapeObjAttachCaption()
    self._verify_caret_moved_into_caption()
    self._ensure_caption_number_exists()

    # '그림 1' 앞뒤로 대괄호와 설명을 덧붙여 '[그림 1] 공동주택관리법 제39조 …' 를 만든다
    self._hwp.Run("MoveLineBegin")
    self._hwp.insert_text(config.CAPTION_NUMBER_OPENING)      # "["
    self._hwp.Run("MoveLineEnd")
    self._hwp.insert_text(config.CAPTION_NUMBER_CLOSING + description)   # "] " + 설명

    self._hwp.Run("SelectAll")
    self._apply_caption_font()
    self._run_alignment(config.CAPTION_ALIGNMENT)
    self._hwp.Run("Cancel")
```

`_ensure_caption_number_exists` — 혹시 번호가 안 들어간 채로 열렸으면 직접 넣습니다. (한글 설정에 따라 다를 수 있으므로)

```python
def _ensure_caption_number_exists(self) -> None:
    """캡션에 번호가 없으면 한글의 '번호 넣기' 로 넣는다."""
    self._hwp.Run("SelectAll")
    if not self._hwp.get_selected_text().strip():
        self._hwp.ShapeObjInsertCaptionNum()
```

**함께 정리할 것**:

- `config.CAPTION_TEMPLATE` 에서 `[그림 {figure_number}] ` 를 뺍니다. 남는 것은 설명 부분뿐입니다.
  ```python
  CAPTION_TEMPLATE = "{law_name} 제{article_number}조 발췌 ({effective_date_label}){same_as_previous_suffix}"
  CAPTION_NUMBER_OPENING = "["
  CAPTION_NUMBER_CLOSING = "] "
  ```
- `core/version_series.py` 의 `build_caption` 에서 `figure_number` 매개변수를 없앱니다. 더 이상 셀 필요가 없습니다.
- `core/pipeline.py` 에서 `figure_number=len(result.succeeded) + 1` 로 번호를 세던 부분을 없앱니다.
- `_clear_automatic_caption_number`(모두 선택 후 지우기)는 더 이상 쓰지 않으므로 지웁니다. 본문을 통째로 지울 뻔했던 위험한 코드였는데, 이번 변경으로 그 위험 자체가 사라집니다. (`_verify_caret_moved_into_caption` 은 남겨둡니다 — 여전히 캡션 안에서 작업하므로)

### G. 붙어 있는 조문 여러 개를 한 장에

**용도**: 제1조와 제2조가 이어져 있을 때 그림 두 장으로 나누지 않고 한 장에 담습니다.

**입력**: 조문 번호 칸에 `1, 2` 또는 `1-3` 처럼 적습니다. 둘 다 받고, `1, 3, 5-7` 처럼 섞어 써도 됩니다.

**받아오는 방법**: 이미 있는 구조를 그대로 씁니다. 저장 창의 조문 목록에서 지금은 하나만 켜는데, 여러 개를 켜면 됩니다. (실제로 아무것도 끄지 않으면 문서 전체가 받아지는 것을 겪었으므로, 사이트가 여러 조문을 함께 내보내는 것은 확인된 셈입니다)

**여러 쪽이 될 때**: 0-2 에서 만든 구조를 그대로 씁니다. 쪽마다 그림 하나씩 만들고 캡션에 `(1/2)` 를 붙입니다. 조문을 여러 개 담아 쪽이 늘어나는 것이나, 조문 하나가 길어 쪽이 늘어나는 것이나 프로그램에게는 같은 일입니다.

**캡션에 조문을 적는 방식**:

| 조문 수 | 표기 | 예 |
|---|---|---|
| 1개 | `제N조` | `제1조` |
| 2개 | `제N, M조` | `제1, 2조` |
| 3개 이상 (이어짐) | `제N조 내지 제M조` | `제1조 내지 제5조` |
| 3개 이상 (떨어짐) | 번호를 나열 | `제1, 5, 9조` |

마지막 줄만 사용자 지침과 다른데, 이유가 있습니다. 제1·5·9조를 골랐는데 `제1조 내지 제9조` 라고 적으면 **중간 조문까지 인용한 것처럼 읽혀** 서면에서 사실과 다른 표기가 됩니다. 이어진 경우에만 `내지` 를 씁니다.

**밑줄 찾기**: 쪽이 여러 개면 모든 쪽에서 찾습니다. 어느 쪽에도 없을 때만 실패로 봅니다.

**고칠 곳**:

| 파일 | 할 일 |
|---|---|
| `core/models.py` | `article_number: int` → `article_numbers: list[int]` (`ArticleCaptureJob`, `ArticleCaptureTask`) |
| `core/law_pdf.py` | 저장 창 목록에서 여러 조문을 켜도록. 파일 이름도 범위로 |
| `core/annotate.py` | 쪽마다 그림 하나씩 만들도록. 두 쪽 이상이면 이름 뒤에 `_2쪽` 을 붙임 |
| `core/article_text.py` | 미리보기도 여러 조문을 이어서 보여주도록 |
| `core/version_series.py` | 위 표대로 조문 표기 만들기 + 쪽 표시 |
| `ui/input_view.py` | 조문 번호 칸 안내를 `예: 1 또는 1, 2 또는 1-3` 으로 |

### H. 가지번호 조문 (제32조의2)

「주택건설기준 등에 관한 규정」을 보니 `제32조의2(지능형 홈네트워크 설비)` 같은 조문이 있습니다. **이 규정 하나에만 26개**나 됩니다 (제9조의2, 제14조의2, 제14조의3, 제16조의2 …). 흔한 형태인데 지금은 넣을 방법이 없습니다.

**확인한 사실**:

| 어디 | 제32조 | 제32조의2 |
|---|---|---|
| 본문 체크박스 값 | `32:0:003200:…` | `32:2:003202:…` |
| 조문 조회 API 의 `JO` | `003200` | `003202` |
| 저장 창 목록 값 (법령) | `0032:00` | `0032:02` |
| 저장 창 목록 값 (고시) | `0032-0000:00` | `0032-0000:02` |

모두 `조 4자리 + 가지 2자리` 형태입니다. `config.py` 의 값 형식은 이미 가지번호를 받도록 되어 있어 그대로 쓸 수 있습니다.

**입력 방식** — `-` 는 범위, `의` 는 가지번호로 뜻을 나눕니다.

| 입력 | 뜻 |
|---|---|
| `32` | 제32조 |
| `32의2` | 제32조의2 |
| `제32조의2` | 제32조의2 (사이트에서 복사해 붙여넣기) |
| `1-3` 또는 `1~3` | 제1조부터 제3조까지 |
| `32, 32의2` | 제32조와 제32조의2 |

`32-2` 처럼 적으면 범위로 읽혀 앞뒤가 뒤바뀝니다. 이때는 **`제32조의2 를 뜻한다면 32의2 로 적어주세요`** 라고 알려줍니다. 조용히 엉뚱하게 처리하지 않습니다.

**자료 구조** — 조문 번호를 숫자 하나가 아니라 두 값으로 다룹니다.

```python
@dataclass(frozen=True, order=True)
class ArticleNumber:
    """조문 번호. '제32조의2' 처럼 가지번호가 붙는 조문이 있어 두 값으로 다룬다."""
    number: int
    branch: int = 0        # 0 이면 가지번호 없음

    @property
    def label(self) -> str:
        return f"제{self.number}조의{self.branch}" if self.branch else f"제{self.number}조"
```

**지금 코드에 숨어 있는 문제 두 가지** — 가지번호를 넣으면서 함께 고칩니다.

1. `core/law_pdf.py` 의 `_read_article_number` 는 체크박스 값에서 **맨 앞 숫자만** 읽습니다. 그래서 `32:0:…`(제32조)과 `32:2:…`(제32조의2)를 구별하지 못합니다. 지금은 우연히 순서상 앞의 것이 잡혀 맞았을 뿐입니다. 값의 첫 칸과 둘째 칸을 함께 읽도록 고칩니다.

2. `core/article_text.py` 의 조문 잘라내기는 다음 조문을 만나면 멈추는데, 조 번호만 비교합니다. 제32조를 요청하면 **제32조의2 까지 딸려옵니다** (둘 다 조 번호가 32라서). 조와 가지를 함께 비교해야 합니다.

**캡션 표기** — 가지번호가 섞이면 줄여 쓰지 않고 온전히 나열합니다.

| 고른 조문 | 표기 |
|---|---|
| 제32조의2 하나 | `제32조의2` |
| 제1조, 제2조 | `제1, 2조` |
| 제1조~제5조 (이어짐) | `제1조 내지 제5조` |
| **제32조, 제32조의2** | **`제32조, 제32조의2`** |

가지번호가 하나라도 있으면 `제32, 32조` 처럼 줄이거나 `내지` 로 묶지 않습니다. 서면에 들어가는 인용이라 정확해야 합니다.

### I. 드래그한 그 자리에만 밑줄 긋기

**지금의 문제**: 밑줄 대상을 '문구' 로만 기억합니다. 그래서 `균열` 을 드래그하면 조문 안의 **`균열` 이 나오는 곳 전부**에 밑줄이 그어집니다. 제7조에는 `균열` 이 열 번 넘게 나오므로 온통 빨간 줄이 됩니다.

**어떻게 고치나**: 문구와 함께 **몇 번째 것인지**를 같이 기억합니다.

드래그한 순간 프로그램은 그 자리가 조문에서 몇 번째 `균열` 인지 셀 수 있습니다. 그 번호를 함께 저장해 두었다가, PDF 에서도 같은 번호의 것만 밑줄을 긋습니다.

**화면에 어떻게 보이나** — 밑줄 칸에 이렇게 적힙니다.

```
균열 폭이 0.3mm 이상인 경우 ; 균열 [2번째]
```

| 적힌 모양 | 뜻 |
|---|---|
| `균열 폭이 0.3mm 이상인 경우` | 그 문구가 나오는 곳 **전부** |
| `균열 [2번째]` | **2번째로 나오는** `균열` 만 |

- **드래그로 고르면** 자동으로 `[2번째]` 가 붙습니다. 딱 그 자리만 그어집니다
- 문구가 조문에 한 번만 나오면 번호를 붙이지 않습니다 (붙일 이유가 없으므로)
- **직접 타이핑하면** 번호가 없으므로 나오는 곳 전부에 그어집니다. 지금과 같은 동작입니다
- 손으로 `[3번째]` 를 적거나 지워서 고칠 수도 있습니다. 사람이 읽고 고칠 수 있는 형태로 둡니다

**개정본이 여러 개일 때**: 판마다 각자의 PDF 에서 2번째 `균열` 을 찾습니다. 개정으로 앞쪽 내용이 바뀌어 그 판에는 2번째가 없으면, 그 건만 `2번째 '균열' 을 찾지 못했습니다` 로 알려주고 나머지는 계속 진행합니다.

**고칠 곳**:

| 파일 | 할 일 |
|---|---|
| `core/annotate.py` | `find_text_areas` 가 '몇 번째' 를 받아 그 하나만 돌려주도록 |
| `core/models.py` | 밑줄 대상을 문구와 순번을 함께 담는 자료로 |
| `ui/preview_view.py` | 드래그한 자리가 몇 번째인지 세어 함께 넘기기 |
| `ui/input_view.py` | `문구 [N번째]` 형태를 읽고 쓰기 |

**왜 이 방식인가**: 드래그한 글자 위치를 그대로 저장하는 방법도 있지만, 개정본마다 위치가 달라 쓸 수 없습니다. '몇 번째' 는 판이 달라도 뜻이 통합니다.

### J. 창에 스크롤바 넣기

미리보기 패널이 들어가면서 창이 세로로 길어집니다. 화면이 작은 컴퓨터에서는 아래쪽 `[실행]` 버튼까지 손이 닿지 않을 수 있습니다.

- 조건 입력 화면 전체를 스크롤되게 감쌉니다 (**세로·가로 모두**)
- **마우스 휠로도** 움직이게 합니다. 스크롤바를 끌지 않아도 되도록
- 창을 크게 늘리면 스크롤바는 저절로 사라집니다

고칠 곳: `ui/app_window.py` — 화면을 담는 자리를 스크롤 가능한 틀로 바꿉니다. 세 화면(입력·진행·완료) 중 길어지는 것은 입력 화면이지만, 틀 자체를 공통으로 두면 나머지도 자동으로 혜택을 봅니다.

### K. 캡션·테두리를 넣을지 고르기

지금은 캡션과 테두리가 무조건 들어갑니다. 서면에 따라 원하지 않을 수 있으므로 체크박스로 고르게 합니다.

```
④ 빨간 밑줄   [균열 폭이 0.3mm 이상인 경우            ]
   ☑ 캡션 넣기    ☑ 테두리 넣기        <- 새로 추가 (둘 다 처음엔 켜짐)
```

**함수 매개변수가 늘어나는 문제**: 지금도 `insert_picture_with_caption(그림, 캡션, 삽입방식, 표시문구)` 로 네 개인데 두 개가 더 붙으면 여섯 개가 됩니다. 매개변수를 셋 이하로 두기로 한 원칙에 어긋나므로, 관련된 값을 하나로 묶습니다.

```python
@dataclass(frozen=True)
class PictureInsertion:
    """그림 한 장을 어떻게 넣을지에 대한 모든 것."""
    image_path: Path
    caption: str | None          # None 이면 캡션을 달지 않는다
    insertion_mode: InsertionMode
    placeholder: str | None
    should_add_border: bool

# 부르는 쪽
editor.insert_picture(insertion)
```

`caption=None` 이 곧 '캡션 없음' 을 뜻하므로 따로 참·거짓 값을 두지 않아도 됩니다.

### L. 연결 — 기존 파일 수정

| 파일 | 할 일 |
|---|---|
| `ui/input_view.py` | 조문 번호 옆에 `[미리보기]` 버튼. 밑줄 칸에 문구를 덧붙이는 통로 하나. 개정본 목록에 전체 선택 체크박스 |
| `ui/controller.py` | `load_article_text(version, article_number)` 를 **별도 흐름에서** 실행 (창이 멈추지 않게). 기존 `drain_messages` 구조 재사용 |
| `ui/app_window.py` | 입력 화면 아래에 미리보기 패널 배치 |

미리보기는 실패해도 전체를 막지 않습니다. 못 불러오면 패널에 "조문을 불러오지 못했습니다" 만 띄우고, 사용자는 지금처럼 직접 타이핑하면 됩니다.

---

## 검증 방법

각 단계 산출물을 실제 파일로 만들어 확인합니다.

```bash
python -m law_capture.core.pipeline --name "공동주택 하자의 조사" --from 2024-01-01 --to 2026-12-31 --jo 1 --underline "제1조(목적) 이 기준은" --hwp out/test.hwp
```

(화면이 아직 없는 2~6단계에서는 이렇게 커맨드라인으로 `core/pipeline.py`를 직접 실행해 결과를 확인합니다. 7단계에서 화면이 붙으면 같은 동작을 창에서 클릭으로 합니다. 시점 하나만 볼 때는 `--from`/`--to` 대신 `--date 2025-06-01`.)

- **버전 선택**: 로그에 `[시행 2025. 2. 3.] 제2025-58호 (구)` 가 찍히는지 → 첫 번째 사진과 대조
- **여러 개정본**: 2024~2026 구간을 주면 2025.2.3.·2026.7.8. 두 개정본이 **오래된 것부터** 처리되는지, 제1조 내용이 같다면 두 번째 캡션에 `(앞 버전과 동일)`이 붙는지
- **PDF**: `out/` 의 PDF를 열어 제1조만 담겨 있고 QR·박스 테두리가 사진과 같은지
- **주석·캡처**: 생성된 PNG를 열어 빨간 밑줄 위치가 사진과 같은지
- **한글**: 결과 hwp를 한/글로 열어 캡션 `[그림 N] 구 공동주택 … 제1조 발췌` 가 그림 위에, 폭이 본문에 맞고, 테두리가 있는지
- **원문 교차검증**: 캡처된 조문 텍스트를 korean-law MCP `get_admin_rule` 결과와 대조해 다른 버전을 잡아오지 않았는지 확인 (개발 중 Claude 세션에서 수행)

### 미리보기 기능 검증

**0번이 가장 중요합니다.** 여기서 막히면 화면으로는 아무것도 못 만듭니다.

0-1. **사용자의 한글을 안 건드리는가** — 이번에 실제 피해가 났으므로 가장 먼저 확인합니다
   - **한글로 아무 문서나 하나 열어둔 채로** 프로그램을 돌릴 것
   - 작업이 끝난 뒤 **그 문서가 그대로 열려 있는지** (닫히면 실패)
   - 그 문서 내용이 건드려지지 않았는지
   - **우리가 켠 한글이 남지 않았는지** (실행 전후 한글 개수가 같아야 함)
   - **빈 문서가 하나만 뜨는지** (`빈 문서 2` 가 같이 뜨면 실패)
   - 결과 한글 파일은 정상으로 만들어졌는지
   - 검증 과정에서 프로세스를 강제 종료하는 명령을 **쓰지 않을 것**
   - 검증용으로 띄우는 한글도 **창을 보이지 않게** 할 것 (화면에 창이 튀어나오지 않도록)

0-1-2. **브라우저가 화면에 안 뜨는가** — 안 보이게 한 상태에서 조문 PDF 가 제대로 받아지는지.
   고시와 법령 양쪽 모두 확인 (저장 방식이 다르므로)

0. **화면에서 [실행] 을 눌러 끝까지 가는가** — 지금까지 한 번도 안 해본 검증입니다
   - 화면을 띄우고 조건을 채운 뒤 실제로 `[실행]` 을 눌러, **한글 파일이 만들어지는지**
   - 사용자가 겪은 `주택법 2011-01-28` 로 재현되지 않는지
   - 이 검증을 `spike/` 에 남겨 두어 다음에도 자동으로 돌아가게 할 것

0-2. **긴 조문이 안 잘리는가** — `주택법 2011-01-28 제2조` (3,084자, PDF 3쪽) 로
   - 그림이 **3장** 나오는지 (지금은 1장만 나오고 나머지가 버려짐)
   - 캡션에 `(1/3)`, `(2/3)`, `(3/3)` 이 붙는지
   - 세 장을 이어 보면 조문 **끝(제15호 리모델링)까지** 다 들어있는지
   - **둘째·셋째 쪽에 있는 문구로 밑줄을 지정해도 찾아지는지** (지금은 첫 쪽만 봐서 실패함)
   - 한 쪽짜리 조문(하자판정기준 제1조)은 예전처럼 `(1/1)` 없이 그대로 나오는지

그 다음으로 중요한 것은 **여러 줄 문구에 밑줄이 제대로 그어지는가** 입니다. 눈으로 확인해야 합니다.

1. **여러 줄 밑줄** — 공동주택관리법 제39조에서 `① 제36조부터 제38조까지에 따른 담보책임 및 하자보수 등과 관련한 제2항의 사무를 관장하기 위하여` 처럼 두 줄 넘게 걸치는 문장을 지정하고, 만들어진 그림에서
   - 줄마다 밑줄이 그어졌는가
   - 각 줄에서 **문구가 끝나는 지점까지만** 그어졌는가 (줄 끝까지 넘어가지 않았는가)
   - 마지막 줄에서 문장이 끝나는 자리에 정확히 멈췄는가
2. **기존 동작이 그대로인가** — 한 줄짜리 문구(`제1조(목적) 이 기준은`)가 예전과 똑같이 나오는지 회귀 확인
3. **텍스트 불러오기** — 고시(하자판정기준 제7조)와 법령(공동주택관리법 제39조) 양쪽에서 조문이 제대로 나오는지. 특히 행정규칙은 조 경계를 잘못 잘라 옆 조문까지 딸려오지 않는지
4. **드래그 동작** — 드래그를 떼면 밑줄 칸에 들어가는지, 두 번 드래그하면 `;` 로 이어지는지.
   **미리보기가 밑줄 칸 바로 아래에 있어서** 드래그한 문구가 들어가는 것이 시선을 옮기지 않고 보이는지 (사진으로 확인)
5. **미리보기 실패 시** — 없는 조문 번호로 미리보기를 눌러도 프로그램이 멈추지 않고 안내만 뜨는지
5-2. **찾기(Ctrl+F)** — 하자판정기준 제7조(항·호가 여러 줄)로
   - `Ctrl+F` 로 찾기 칸이 뜨고 커서가 그리로 가는지
   - `균열` 을 넣으면 여러 곳이 노란색으로 표시되고 개수가 맞는지
   - `Enter` 로 다음 곳으로 넘어가며 화면이 따라 스크롤되는지, 마지막에서 처음으로 돌아오는지
   - `Esc` 로 닫으면 표시가 깨끗이 지워지는지
   - 찾기 표시(노랑)와 드래그로 고른 표시(옅은 빨강)가 섞이지 않는지
   - 없는 글자를 넣으면 팝업 없이 안내만 나오는지
5-3. **'어느 시점?' 세 가지 방식** — 「지능형 홈네트워크 설비 설치 및 기술기준」과 「주택법」으로
   - **특정 시점 하나**: 날짜 칸이 **하나만** 남고 `기준일` 이름표가 붙는지.
     `2012-09-20` → **시행 2011. 3. 4.** 판 하나가 나오는지 (다음 개정이 2013. 4. 12. 이므로).
     `2011-03-03` → 그 전 판인 시행 2009. 8. 24. 가 나오는지 (경계 확인)
   - **기간**: 칸 두 개가 다시 나오고 그 사이 개정본만 나오는지
   - **전체 기간**: 날짜 칸이 사라지고, 주택법이면 **172개** 가 전부 나오는지.
     이때 **처음부터 전부 꺼져 있는지**, `필요한 것만 남기고 체크를 꺼주세요` 안내가 뜨는지
   - 172줄이 창을 밀어내지 않고 **목록 안에서 스크롤** 되는지
   - `[전체 선택]` 으로 172개를 한 번에 켜고 끌 수 있는지
6. **전체 선택/해제** — 개정본이 여러 개인 법령(공동주택관리법 2024~2026, 8개)으로
   - 전체 선택을 끄면 전부 해제되는지
   - 개별 항목 하나를 끄면 전체 선택 표시도 꺼지는지
   - 선택 개수 표시가 실제와 맞는지
7. **캡션에서 '구' 빠졌는지** — 옛 판으로 만든 그림의 캡션이 `[그림 1] 공동주택 … 제1조 발췌 (시행 2025. 2. 3.)` 인지 (앞에 `구` 가 없어야 함). 화면의 개정본 목록에는 `(구)` 가 그대로 남아 있는지
8. **붙어 있는 조문 여러 개** — 하자판정기준으로
   - `1, 2` 를 넣으면 그림 **한 장**에 제1조와 제2조가 함께 들어가는지
   - `1-3` 도 같은 결과인지 (입력 방식 두 가지 모두)
   - 캡션이 `제1, 2조` / `제1조 내지 제3조` 로 나오는지
   - 떨어진 조문(`1, 5, 9`)을 넣으면 `제1, 5, 9조` 로 나오는지 (`내지` 를 쓰면 안 됨)
   - **조문을 많이 넣어 두 쪽이 될 때** 그림이 두 장 나오고 캡션에 `(1/2)`, `(2/2)` 가 붙는지, 내용이 잘리지 않았는지
   - 밑줄 문구가 둘째 쪽에 있어도 찾아지는지
8-2. **가지번호 조문** — 「주택건설기준 등에 관한 규정」으로
   - `32의2` 를 넣으면 **제32조의2(지능형 홈네트워크 설비)** 가 나오는지
   - `제32조의2` 라고 그대로 붙여넣어도 되는지
   - **`32` 를 넣었을 때 제32조만** 나오고 제32조의2 가 딸려오지 않는지 (지금 있는 문제)
   - `32, 32의2` 를 넣으면 캡션이 `제32조, 제32조의2` 로 나오는지 (`제32, 32조` 가 되면 안 됨)
   - `32-2` 를 넣으면 `32의2 로 적어주세요` 안내가 나오는지
   - 미리보기에서도 제32조의2 내용이 제대로 나오는지
8-3. **드래그한 자리에만 밑줄** — 하자판정기준 제7조(`균열` 이 여러 번 나옴)로
   - 두 번째 `균열` 을 드래그하면 밑줄 칸에 `균열 [2번째]` 로 적히는지
   - 만들어진 그림에서 **그 한 곳에만** 밑줄이 그어졌는지 (나머지 `균열` 에는 없어야 함)
   - 손으로 `균열` 만 적으면 예전처럼 나오는 곳 전부에 그어지는지
   - 한 번만 나오는 문구를 드래그하면 `[1번째]` 가 안 붙는지
8-4. **창 스크롤** — 창을 세로로 작게 줄였을 때
   - 스크롤바가 나타나 `[실행]` 버튼까지 닿는지
   - 마우스 휠로도 움직이는지
   - 창을 다시 키우면 스크롤바가 사라지는지
9. **캡션·테두리 고르기**
   - 처음엔 둘 다 켜져 있는지
   - 캡션을 끄면 그림만 들어가고 번호가 안 붙는지
   - 테두리를 끄면 선이 없는지
   - 캡션만 끄고 테두리만 켠 경우도 정상인지
10. **캡션 번호가 한글 자동번호인지** — 이것이 이번 변경의 핵심이므로 반드시 확인
   - 그림 두 장을 넣었을 때 `[그림 1]`, `[그림 2]` 로 나오는지
   - **한글에서 결과 문서를 열어 번호 위에 커서를 놓았을 때 필드로 표시되는지** (회색 배경 등)
   - **이미 그림이 들어있는 문서에 넣었을 때** 그 다음 번호부터 이어지는지 (1번부터 다시 시작하면 안 됨)
   - 한글에서 첫 번째 그림을 지웠을 때 남은 그림 번호가 저절로 앞당겨지는지
